# Perch 1.2: whole-app review

Status: in progress. The user explicitly assigned the full review **and its resulting changes** to version 1.2 on 2026-09-06. The 1.1 local feature release is recorded in [RELEASE-1.1.md](RELEASE-1.1.md). Incremental fixes and safe test passes do not constitute completion of this review.

## Keyboard/accessibility implementation — build 81 candidate

Completed the reviewed settings implementation gaps: local key traversal, long
explanation scrolling, page identity/change notifications and remaining ambiguous
accessible labels. Native lab keyboard journeys pass; the earlier unexplained
Tab behavior is now traced to the system key-view preference and handled locally.
No OS keyboard or VoiceOver preference was changed. Spoken VoiceOver and expanded
production-route acceptance remain release tests, detailed in
[ACCESSIBILITY-REVIEW-81.md](ACCESSIBILITY-REVIEW-81.md). This does not close other
release, hardware or feature work. Build 77 remains installed.

## Installed-copy restart — September 9, build 80 candidate

Added a delivery-independent installed-copy check, conditional running/on-disk
menu information and restart command, and a deferred once-per-process notice.
The existing restart safety mechanism is reused. All 22 isolated suites pass;
no live app replacement or active lid handoff is claimed. See
[APP-REPLACEMENT-REVIEW.md](APP-REPLACEMENT-REVIEW.md). Build 77 remains installed.

## Whole-app checkpoint — September 9, build 79 candidate

The safe portion of backlog item 9 is complete, with findings and journey coverage
in [WHOLE-APP-REVIEW-79.md](WHOLE-APP-REVIEW-79.md). One P1 recovery-ordering defect
and four P2 groups were fixed. All 21 isolated suites passed, and limited real
native-lab interaction passed with AGENT MODE. Keyboard-only/VoiceOver acceptance
is still explicit, alongside the existing permission, lid, collector and clean
installation checks. Source review is not a claim of native input on every route.
The installed app remains build 77; no live hardware/permission mutation occurred.

## Tooltip coverage and consistency — September 9, build 78 candidate

The user confirmed todo item 2 (native menu interaction) works, then reported
missing and inconsistent tooltip content. Source inspection found missing help
on five action rows, refreshes replacing purpose with status (including empty
Settings help), technical keyboard copy and a misleading immediate-action claim
for the confirmed Panic menu route. These are corrected in source across all 21
action rows and related Settings controls; coverage is in
[TOOLTIP-REVIEW.md](TOOLTIP-REVIEW.md). Action construction now requires explicit
help. Shared copy avoids menu/Settings divergence; current state is supplemental.
The review skill now checks content coverage separately from hover mechanics.
Build 77 remains installed; no new native hover/content acceptance is claimed.

## User acceptance and shared shortcut label — September 9

The user confirmed todo item 1 (build 77 Back navigation) works. This closes the
reported result-Back defect; it does not assert agent-observed input on every
sibling route. The user also identified inconsistent Escape/Esc spelling.
PanicShortcut now supplies Esc in its shared key list and a single title for
Settings, test prompts, menu hints and accessibility. The menu-only replacement
formatter was removed from both Panic and monitor consumers. Key codes, saved
configuration and shortcut behavior are unchanged.

## Current follow-up — September 9, build 77 installed

The user verified shortcut detection and identified broken Back on its success
and timeout results. X starts cleanup, the Agent Kill Switch parent responds,
and Add app/executable pickers work. All app-owned alerts now use asynchronous
shared-window completion instead of nested native modal loops. The hidden
production-flow test dispatches Back without a response driver and passes
success/timeout/cancel/X/cleanup/repeated-parent cases. This does not establish
the original native event failure's exact cause or replace physical acceptance.
See [the updated whole-set static audit](DIALOG-INTERACTION-REVIEW.md).

The sweep also fixed an unbounded return-to-overview loop when a draft refuses
Back. The shared header's two draft validators and all deliberate Back-disabled
states were inspected. A build gate blocks production use of the old synchronous
fixture adapter. Fresh shortcut configuration is checked as Control–Option–
Command–Escape; menu rendering uses `Esc`, retaining saved choices.

The static pass prepared build 77 separately. The user subsequently authorized
installation, which is now complete. The current ordered backlog is in
[TODO.md](TODO.md); it supersedes older open-item wording without erasing the
historical evidence below.

### Build 77 installation — September 9

Strict nested/app signatures and the designated requirement matched the previous
installation. Build 75/PID 6898 was stopped after verifying its exact identity;
its bundle was retained in the task's work directory. Build 77 was installed at
`build/Perch.app` and launched through LaunchServices as PID 61677. Guardian/input
helpers updated to build 77 (PIDs 61702/61705). Root lid supervisor/watchdog PIDs
80843/80846 remained unchanged, using compatible helper revision 2.

All saved preference keys compared equal and Safety/config.json was byte-identical
before/after. The lid was closed on AC; persistent/live SleepDisabled were off
and both runtime and durable ownership records were absent before and after.
There was no active lid session to transfer. AGENT MODE covered installation and
was stopped afterward. Native Back/hover checks were not performed. No permission
reset, panic, hardware change, sleep test or public release occurred.

## Previous follow-up — September 9, build 76 candidate

- **Confirmed by user on build 75:** menu flickering is resolved. After the
  physical keyboard checklist was explained, the user confirmed those behaviors
  work. Record this as user acceptance, not an agent-observed replay of each
  combination. Sleep/recovery and the dead-button dialog remain separate.
- **P2 tooltip report, source correction:** Panic help appears over other rows.
  Custom rows now own their view tooltips; native item tooltips are cleared.
  Hover refresh preserves tracking areas it does not own. Actual hover behavior
  has not been reproduced or confirmed fixed by the agent.
- **P2 selection mismatch, source correction:** keyboard activation used native
  highlightedItem after custom pointer navigation cleared its visible highlight.
  Return/Space now activates the visible row and cannot fall through to a stale
  native selection when a row is hidden/disabled. Hidden transition tests pass.
- **P2 deferred action race, source correction:** a dismissed command retained
  its old selector but used the current target after a refresh. Dispatch now
  requires both selector and target to remain unchanged; hidden tests pass.

The native/custom boundary audit also traced native validation, Cmd-Q versus
global shortcut display, dismissal/reopening, allocated width, accessibility
state/help, conditional rows and ordinary native popup items. No further source
defect was established in those paths. Native hover/navigation acceptance is
still required. Build 76 is prepared separately; installed build 75 is unchanged.

## Dialog buttons — September 9, build 75

**P1 acceptance open:** user encountered another dialog whose buttons did not
work and exited with X. The user now recalls Agent Kill Switch shortcut setup
and possibly a child dialog; the exact screen remains unconfirmed. The
[whole-set interaction audit](DIALOG-INTERACTION-REVIEW.md) includes a focused
editor/test/result/cleanup replay checklist and
records 58 UI construction/entry sites. Source corrections scope timer/button
completion to its alert, clear external-handoff ownership on parent close,
exclude background display/title updates during an interaction and remove
obsolete global modal-stop handlers. New hidden hit-test/action fixtures pass.
A source gate and retained native smoke app prevent treating mocked responses
as complete interaction evidence. The Mac is locked, so real clicks and the
visible full suite remain unverified. This is not a claim that the reported
route was reproduced or that all native dialogs are now confirmed working.

### Build 75 installed — September 9

User-authorized installation completed at `build/Perch.app`. Before stopping
build 70/PID 85188, persistent and live SleepDisabled were both off, runtime and
durable lid ownership records were absent, and AC power was connected. Therefore
no active lid-session handoff was needed or tested. The separately prepared
build 75 passed strict nested signature verification with the same designated
requirement. The old app was retained in this task's work directory as a backup,
and build 75 was launched through LaunchServices as PID 28415.

Guardian/input helpers updated to build 75 (PIDs 28422/28425). The root lid
supervisor/watchdog remained PIDs 80843/80846, using helper revision 2. All saved
preference keys compared equal before/after, including `sleep.includeLid=true`;
the inactive lid session stayed inactive and SleepDisabled stayed off. Ordinary
Perch sleep prevention was present after startup. AGENT MODE covered installation
and was stopped afterward. No permission reset, panic, monitor command, sleep
test or public release occurred. Real-click dialog checks and physical acceptance
remain open; installation alone does not close them.

## Current bugs and acceptance — September 9, build 74 candidate

- **Ordinary Keep awake flicker: source correction.** SleepStatus previously
  counted transient same-process activity assertions as Perch's explicit request.
  It now requires the named sleep-prevention assertion. Menu polling derives one
  final sleep presentation and leaves unchanged rows alone, instead of applying
  temporary labels/state/enabled flags before the final lid presentation.
  Visual acceptance on the user's machine remains open.
- **Saved lid choice: implemented.** Menu and Keep awake show saved intent even
  after a session stops. Current protection is reported separately; stale status
  stays unknown. Clicking the checked, inactive choice clears that preference
  without enabling the master or starting a session. Keep awake includes an
  explicit Resume action; saved intent alone never rearms protection.
- **Conditional wake explanation: implemented in the menu app.** When the saved
  choice is on and the lid is observed closed at the will-sleep notification,
  capture a small persistent incident. On wake, explain the recorded sequence
  with View lid activity and Close. Distinguish recorded expiry, lost supervision,
  inactive/unconfirmed protection and unexplained sleep without claiming a
  command proves causality. Bound evidence to the interval, require corroborating
  sleep/wake records after a process restart, expire incidents after 24 hours and
  persist acknowledgement. Ordinary sleep with the choice off produces no notice.
  A menu app that was absent before sleep cannot reconstruct the exact saved
  preference at that event and does not invent a notice. Real notification order,
  actual sleep and restart recovery still need physical acceptance.
- **Launch-access recovery: implemented; exact attribution remains a limit.**
  A startup check warns once for a blocked connected external keyboard. Menu,
  Keyboard settings/details, navigation learning and Setup & status share the
  already-enabled recovery, with an exact-copy Finder route. Actual Automation
  denial gets its own scoped explanation; cancellation does not. Public access
  checks establish current denial, not TCC's responsible application. No parent-
  PID guess, private API, log privilege or permission reset is introduced. The
  [feature/process audit](LAUNCH-ACCESS.md) keeps independent helper permissions
  separate. The affected machine still needs a normal-launch recovery replay.
- **Quit / Command-Q corrected:** user confirmation under both modifier
  combinations superseded the automation-only uncertainty. Source had no
  application menu for Settings shortcuts; custom status-menu handling omitted
  its displayed Quit shortcut. Both routes now use the same quit action.
  Isolated dispatch checks pass; Command-Q from installed build 70 Settings
  exited the exact app process in repeated live checks.
- **External F1–F12 access recovered without permission changes:** MX Keys was
  recognized, but direct agent-shell launches (PIDs 83244/84784) inherited Codex
  as the TCC responsible identity. Perch was already enabled in Input Monitoring,
  as the user correctly reported. Normal LaunchServices launch (PID 85131)
  restored an enabled, checked Fn control immediately. In-app Restart then
  launched PID 85188, retained Perch attribution and kept Fn available/on.
  This establishes recovered access and observed firmware mode, not a physical
  Fn-key/write test. The unavailable-state UI must explain the reason and repair
  beside the control, include the already-enabled case and show unknown rather
  than falsely off. AGENTS.md and the flow skill capture the launch lesson.
- **Confirmed working by the user:** CPU colors and password entry.
- **Product correction:** local-file Updates page replaced by direct Restart
  Perch in App settings. Installed build 70 restarted successfully twice with
  an inactive, closed-lid session. No override or helper replacement occurred.
  Required helper updates stay in Keep awake / Setup & status. A release update
  checker remains a separate deferred feature.
- **Validation gaps, not established defects:** supervisor/watchdog failure,
  independent recovery after both fail, reboot, deadline-edge/power-flap cases,
  active-session restart handoff and remaining physical keyboard tests.
  Menu-process loss and explicit disable passed as recorded below.

### Build 74 remote work

The app candidate is built separately from the installed app. Root helper
revision/protocol and power behavior are unchanged. The active build 70 menu app
and supervisor/watchdog remain untouched. This pass does not exercise real sleep,
change permissions or monitor/keyboard hardware settings, inject live failures,
or publish a release. All 20 isolated suites are covered by passing runs: 19 in the full run and
AppKit settings in the final targeted rerun. The latter includes assertion
classification, unchanged rendering, saved intent and real toggle-handler
semantics with injected power observations, conditional/durable/deduplicated
notices, temporal evidence, access-notice gating and Back/reopen. Earlier failures
exposed an obsolete unknown-preference expectation, live power-read leakage in
one new fixture and a 30ms callback timing assumption; these were corrected without
weakening production safety or changing live settings. Light/dark recovery
renders were inspected. Isolated compilation completed with zero warnings.
The flow skill now includes stopped-session intent/action/notice checks.
Build 74 production compilation also had zero warnings. Strict app/nested
signatures and the build-74 ZIP integrity/version checks passed. The candidate
is signed and packaged separately; installation remains deferred.

### Build 73 preparation and focused flow evidence

Build 73 is signed and packaged. Production/isolated compilation had zero
warnings. The suite pass covers all 20 suites; after fixing fixture setup,
a superseded recovery-route assertion and live durable-ownership leakage, the
changed AppKit suite passed its targeted rerun. The other 19 suites passed in
the preceding run. Native tests used AGENT MODE and it was stopped afterward.
At completion, build 70/PID 85188 stays installed and running with the now-active
lid session; root supervisor/watchdog PIDs 80843/80846 were not restarted. Build
73 installation is deferred to preserve that active session. The source changes
and updated todos are committed separately from any public release.

| Journey | Evidence and remaining boundary |
| --- | --- |
| App settings → Restart → result / retry / Back | Real inactive closed-lid restart twice on 70, exact old-process exit/new receipt; isolated unchanged bundle, cancellation, failure/retry and result retention. Active handoff remains a physical acceptance gate. |
| Setup & status → Keep awake → helper maintenance | Fixture pending/closed/open/busy/failure/Back states, direct recovery route; no privileged helper installation during this pass. |
| Keyboard settings / menu → access recovery → ready | Live MX Keys access restored by normal app launch and retained across Restart. Fixture denied/unknown state, direct OS route and ready-state restoration; light/dark renders. Physical Fn write/key tests remain. |
| Quit from Settings / custom status menu | Actual Settings Command-Q exit plus isolated dispatch through both menu routes; no global shortcut registration or status-menu redesign. |

## Monitor direction changed — coordinated Perch KVM replaces cycling

The user explicitly retired standalone monitor input cycling as the intended
feature. Replace its remaining product/acceptance work with the
[coordinated Perch KVM todo](KVM-PLAN.md): paired computers communicate before
switching displays, transfer mouse control and keyboard focus independently of
which host physically receives the inputs, support hotkey handoffs and optional
handoff when the same registered keyboard switches hosts (MX Keys-style 1/2/3),
and handle one monitor, either/both of two and mixed display/computer arrangements.
Peer coordination, input forwarding and reliable keyboard identity/detection
still need design and feasibility validation; none is claimed implemented.

**Keyboard testing stays on the todo**, including physical Fn modes, modifier
swaps, navigation keys, recognition/learning and safe shortcut delivery. The
Command-Q failure is now confirmed by the user with both modifier combinations;
verify the correction separately from the remaining keyboard mapping tests. Historical monitor
findings below describe existing code, not a requirement to finish the retired
cycling experience before starting the replacement. This change updates planning
only; it does not remove working code or alter live hardware.

## Helper 69 explicit disable — normal cleanup confirmed

The user performed two enable/disable pairs with the lid open on battery.
Commands were accepted at 22:48:15 and 22:49:04; disable requests at 22:48:28
and 22:49:08 completed release in roughly 40 milliseconds each. The helper
recorded successful disable both times. Afterward persistent/live SleepDisabled
were false, runtime ownership and the durable journal were absent, and ordinary
Perch idle-sleep prevention remained active. Independent recovery continued
exiting successfully. No new system sleep was recorded during these open-lid
disable tests. Root-supervisor/watchdog failure and reboot recovery remain
unverified; these successful normal disable paths do not establish them.

## Helper 69 menu-process exit — heartbeat cleanup confirmed

With user-authorized testing, the lid open and both override readings on, a
SIGTERM was sent only to the verified menu-app PID 80665. The input/safety helper
PIDs 80669/80672 and root supervisor/watchdog PIDs 80843/80846 remained running.
Polling observed SleepDisabled 0 and both ownership records absent within 5.084
seconds of signaling. The watchdog recorded release and the supervisor recorded
app-heartbeat expiry. No new sleep was recorded with the lid open. The app was
relaunched as PID 82131; live Keep awake showed ordinary keep-awake still on,
lid protection off and an explanation that the stopped session needs explicit
enabling. Both native override readings remained false after relaunch.
AGENT MODE was stopped after verification. This establishes menu-process loss
recovery in this run, not root-helper failure or reboot recovery.

The attempted Command-Q from the live Settings window did not exit the app in
this automation run. The loss-of-process test therefore used SIGTERM rather
than claiming normal Quit-menu acceptance. Review shortcut routing from Settings
and perform the actual Quit menu action separately; keep that usability issue
distinct from the confirmed heartbeat cleanup.

## Helper 69 full timeout — actual sleep and cleanup confirmed for this run

The user kept the lid closed on AC from 22:39:13, unplugged at 22:42:13, and
reopened at 22:43:33. The independent watchdog reached its observed 60-second
battery deadline at 22:43:13.199, released the override successfully at .247,
then requested sleep. Perch received the system-will-sleep notification at .283.
macOS recorded entering Software Sleep at 22:43:18, attributed to watchdog PID
80846, followed by DarkWake at :28 and full wake on user activity at :33.
Thus cleanup and the sleep request occurred at the deadline; the OS sleep
transition finished roughly five seconds later.

After opening, persistent/live SleepDisabled were false and both runtime
ownership and durable recovery journal were absent. The independent recovery
job continued exiting successfully; the live Lid activity page reported the
session off. No new session was silently enabled. AGENT MODE covered only that
UI inspection and was stopped afterward. This confirms the full timeout and
normal watchdog cleanup in this run. Explicit disable, menu exit, supervisor/
watchdog failure, reboot recovery and active update handoff remain acceptance
gates; successful nominal runs do not establish those failure cases.

## Helper 69 first physical checks — powered close/open and short battery grace

Third run, the requested unplugged/open → close → plug → open sequence:
closing on battery at 22:37:47 started the countdown; reconnecting at 22:37:58
paused enforcement after 10.7 seconds, and five stable seconds on AC cleared
the interval. The lid opened at 22:38:06 after 18.8 seconds closed. No macOS
Sleep/Wake entries or Perch sleep notifications were recorded. Both override
readings stayed true and independent recovery remained healthy. powerd again
cleared its shared clamshell flag after reconnect, but this run did not record
the earlier unwanted sleep. Full expiry and failure cleanup remain unverified.

Second run: the user closed while plugged in at 22:36:30, unplugged at 22:36:34,
then opened at 22:36:46 while remaining on battery. Perch started a 60-second
countdown on unplug and canceled it on opening after 11.2 seconds of battery
grace (15.2 seconds closed in total). No macOS Sleep/Wake entries or Perch sleep
notifications were recorded. Persistent/live SleepDisabled remained true and
independent recovery continued successfully. This supports the short
undock/open case; reconnect and actual expiry still need testing.

The user enabled lid protection while plugged in with the lid open. Both the
persistent and live SleepDisabled readings became true; the root ownership
journal and renewing recovery lease were present. Recovery continued completing
successfully without ending the healthy session. The user then closed the lid
at 22:35:08 and reopened it 10.2 seconds later. Perch and powerd recorded the
close/open transitions; no macOS Sleep/Wake events or Perch sleep notification
were recorded in this test window. Both override readings remained true after
opening, and recovery remained healthy. This supports the powered close/open
case for this run only. Battery transitions, 60-second expiry, explicit disable
and coordinated crash/reboot cleanup still require physical acceptance.

## Helper 69 installed — activation and physical acceptance next

After the user's administrator authorization, the installed root helper is build
69/revision 2. Its supervisor and watchdog are running. The separate recovery
job is loaded with RunAtLoad and a five-second interval; four observed runs had
exit code 0. The live Updates page confirms “Lid helper updated and responding.”
Keep awake is on, including-with-lid-closed is off, the lid is open and both
persistent/live SleepDisabled are false. AGENT MODE was used for UI inspection
and then stopped; no power setting was changed. The read-only baseline was
captured before asking the user to enable protection with the lid still open.
This verifies installation and idle recovery execution, not active cleanup,
crash/reboot recovery or physical sleep prevention.

## Live app update and remaining Updates usability — build 69

The user reported that build 68's Updates might not work and asked for direct
testing. Its page contained the generic rejection “Choose a newer Perch app that
supports this update handoff.” The previously selected file was not established.
Selecting the prepared, signed build 69 through the real file picker succeeded;
Update & restart replaced the app and the new process showed “Updated to build
69. Your saved choices were kept.” The installed version and strict signature
were verified. The lid was open, SleepDisabled was 0 and no active-session or
durable override marker existed. This verifies an inactive update, not a live
lid-session handoff. The privileged helper remains revision 1/build 68, with
Finish helper update available for the user's administrator authorization.

**P2 — Updates still assumes knowledge of where the new app is.** This local
updater does not find/download a release, yet its first action opens a file picker
without a prepared candidate's location. The agent had left build 69 in a task
working folder and had not supplied that location. The page should clearly
identify the current build, explain the local-file requirement, and make a
delivered candidate easy to select. Distinguish same/older build, wrong app and
unsupported protocol errors; the current combined rejection hides the recovery
step. The user should not have to infer these details. This usability follow-up
is open; it was not repaired by successfully performing the update once.

## Guarded lid replacement — build 69 installed

The user authorized an all-system sleep override when necessary, provided it has
independent recovery. The powerd/shared-bit P1 has a replacement implementation
in helper revision 2, documented in [LID-RECOVERY.md](LID-RECOVERY.md). It journals
ownership before enabling, refuses existing overrides, verifies persistent and
live state, serializes bounded writes, revokes stalled writers during cleanup,
and installs an independent boot/periodic recovery job. Failed cleanup retains
the journal and retries. Helper installation keeps the old recovery job until
cleanup succeeds. Manual Sleep is blocked while enabled and the UI explains it.

Build 69 compiled with zero production or isolated warnings. All 20 suites
passed under AGENT MODE, including read-only native power observations, durable
ownership reconstruction, failed-cleanup retention, lease/boot/process identity,
a stopped disposable writer and unplugged/open → close → plug → open policy.
Light/dark Keep awake renders passed inspection. No live power mutation or
privileged helper replacement was performed in this validation. **P1 physical
acceptance remains open**, including actual override application/removal,
reconnect, expiry, and coordinated crash/reboot cleanup. Do not describe this as
an absolute guarantee against a stranded setting if the OS or recovery job cannot
run. Historical sections below describe the earlier shared-flag implementation.

## Menu refresh follow-up — build 69 installed; ordinary Keep awake flicker reported

**P2, open user report after installation:** the ordinary **Keep awake** menu
item now flickers. This report concerns the normal idle-sleep control, not the
Including with lid closed row. Add it to the live menu-refresh investigation
alongside the earlier CPU-color and F1–F12 availability symptoms. Determine
whether its checkmark, enabled state, text/color or geometry is changing during
routine refresh; the exact visual change and cause have not yet been captured.
Do not mark overall menu stability resolved based on the keyboard/color fixes
or successful lid tests. No implementation change was made for this report.

The user confirmed two remaining glitches: CPU text changes color after updates,
and both F1–F12 controls temporarily disable. **P2, fixed in candidate sources:**
keyboard presentation polling every two seconds was classified as blocking work.
Routine reads now retain confirmed values and availability, while first discovery,
device invalidation and mutations remain distinct. Built-in Fn changes join the
same serial worker as reads and external Fn changes; a click during a read queues
the desired value. Unchanged background results do not republish the Settings
page. Polling remains enabled. Failed reads replace the result with unavailable
or repair state rather than silently keeping stale readiness.

**P2, fixed in candidate sources:** System-row colors were selected by searching
the whole assembled text for words such as Unavailable and Critical. Optional
process attribution could therefore gray valid CPU usage, and a process name
could incorrectly select warning colors. System readings now carry explicit
severity. CPU availability depends on its primary measurement, independently of
optional process attribution. Memory and thermal severity use their actual state.

The sibling audit covered System readings, agent protection/resume, monitor
cycling, audio, scrolling, modifiers, navigation, Keep awake/lid and login.
Helper and lid queries already retain a fresh snapshot during routine requests;
expiry/errors invalidate it. Monitor readiness survives an unchanged menu open,
with topology changes invalidated before debounce and writes serialized. Audio,
login and modifier reads do not introduce an asynchronous checking transition.
No action was made usable with unknown initial or invalidated device state.

Validation: all 20 isolated suites passed under AGENT MODE, including an injected
held keyboard read followed by a queued user change, device invalidation, and
CPU/process-name/unavailable-attribution colors across six appearances. No live
keyboard or power change was made. These source fixes are not installed yet.
The reusable flow skill now covers read-versus-write availability and typed color.

## Controlled updates and remaining acceptance — build 68, September 8, 2026

The user authorized a separate, carefully tested app-restart handoff and deferred
lid-helper updates, with a persistent notice. Implementation and its verification
boundaries are recorded in [UPDATES.md](UPDATES.md). The first upgrade from the
installed build 63 needs the lid open; that helper does not implement the new
protocol. Compatible later app updates can preserve the supervised session while
helper replacement waits for an explicit open-lid Finish helper update action.
The shared macOS clamshell-flag defect is not fixed by update handoff work.

Build 68 was prepared separately and is now installed. Production compilation
and strict signatures passed with zero compiler warnings. All 20 isolated suites
passed under AGENT MODE, including a signed disposable worker's actual file
replacement, relaunch and acknowledgment. The worker test caught a file-URL
representation mismatch in app identity comparisons; normalized filesystem paths
now agree without weakening the code-hash check. Code-signature validation accepts
build 68 and rejects build 67 against the ticket's exact new-app hash. The updater
also launches and tracks the exact executable, and bounded descriptor reads avoid
unnecessary extended-attribute queries during restart. Native queued, ready and
recovery states were inspected in light/dark renders. A live privileged handoff
and physical sleep behavior remain acceptance gates.


Live bootstrap observation at 21:17 on September 8: the installed app, input/safety
helper and privileged lid-helper bundles are all build 68. The root lid helper
logged startup at 21:01:23 with an open lid and battery power; its supervisor and
watchdog processes are running. No active-session marker or enable event was
present in the read-only capture. The user reported completing authorization.
Live Settings inspection timed out, so a fresh authenticated status and the
on-screen success result are not yet verified. This establishes installation and
startup, not successful physical lid protection or first-attempt password entry.
AGENT MODE was stopped after inspection; no sleep test was started.


Remaining priorities, with completed source fixes distinguished from acceptance:

- **P1: actual lid/sleep reliability.** Coordinate undock/open, short reconnect,
  closed-battery 60-second expiry, late reconnect and wake with the user; correlate
  Perch's journal with OS evidence. Shared powerd interference and short fail-safe
  paths remain separate candidate causes. A native timed-assertion replacement
  remains research, not an implemented or approved escape hatch. Apple’s current
  [power-manager implementation](https://github.com/apple-oss-distributions/PowerManagement/blob/main/pmconfigd/PMAssertions.c)
  still explicitly checks entitlements for both AppliesToLimitedPower and
  AppliesOnLidClose; rereading it did not establish a third-party replacement.
- **P1: update/helper acceptance.** Bootstrap installation completed; first-attempt password typing and live UI result still need confirmation;
  live compatible app handoff; unchanged battery deadline; missing app/expired
  claim; pending helper with the lid closed, opening and completion. No live
  watchdog kill or forced sleep should occur without coordinated timing.
- **P2: installed menu and system-dialog acceptance.** Recheck the reported
  periodic menu glitch after installing the new candidate; secure password entry
  on first attempt and recovery on the affected Mac are still hardware/UI gates.
- **Replacement feature: coordinated Perch KVM.** Standalone monitor-cycling
  acceptance is superseded by [KVM-PLAN.md](KVM-PLAN.md), including shared display
  arrangements and coordinated mouse/keyboard focus across paired computers.
- **P2: physical keyboard/input acceptance remains.** Test real Fn modes,
  Control/Command mappings, navigation keys, recognition/learning, device changes
  and safe shortcut delivery. No actual Panic or privacy reset is part of this run.
- **P2: native collector migration.** Install the launcher, confirm event delivery,
  CPU attribution, authorization requirements and restart/reboot recovery. The
  installed legacy collector still collects; source implementation is complete.
- **Release gates:** clean install/uninstall, longer performance/load and recovery,
  accessibility, dependency/license provenance, supported hardware evidence and
  distribution/Homebrew packaging. No public publication is authorized here.

### Build 68 physical acceptance — September 8

The live Updates page confirmed “Lid helper updated and responding.” Keep awake
was on and lid protection initially off. The user connected external power and
enabled lid protection; the helper recorded a new session at 21:28:35 and command
acceptance. AGENT MODE bracketed UI inspection and was off for user-operated tests.

| Scenario | Observed result | Scope |
| --- | --- | --- |
| Close on power, undock, open within 60 seconds | Lid closed at 21:29:51, unplug observed at 21:29:58, 60-second countdown started, lid opened at 21:30:10. Countdown cancelled after 12.2 seconds on battery. User reported immediate resume. No sleep/wake interruption was recorded in the helper journal or filtered macOS history. | This run behaved as intended. One successful run and absent sleep records do not resolve the intermittent shared-control defect. |

**P1, second physical run failed despite immediate return.** The user performed
close-on-power / disconnect / short reconnect / open and reported immediate
return. At 21:33:04 the helper observed lid closure and macOS beginning sleep,
with an active session, no battery countdown and no Perch sleep request. It
stopped the session as designed. The OS power log independently records
**Clamshell Sleep on AC at 21:33:09**, a wake at 21:33:19, notification wake back
to sleep at 21:33:23, and wake/open at 21:33:31. Thus immediate return and no
password screen did not establish continuous wakefulness. The helper's queued
unplug/reconnect observations were 0.2 seconds apart after sleep began; that is
not a measurement of the user's physical cable interval.

The same Mac's powerd log supplies the missing external-writer evidence:
reconnecting power while open at 21:32:43 triggered clamshell reevaluation;
`EvaluateClamshell. Disable : 0` was logged at 21:32:44 with desktop mode and
eligible assertion counts both zero. At lid closure, powerd reported
`isSleepDisabled : 0`, then Clamshell Sleep. Perch had issued its sole prevention
request at 21:28:35 and retained its local `preventing` flag until the sleep
notification. This directly corroborates the shared-control enforcement gap for
this run; the original other-Mac incident remains independently unconfirmed.
No preceding app-heartbeat or watchdog failure appears in the captured journal.
The update path was not running during this test. No speculative reassertion
patch was applied: periodic writes still race a closed-lid OS reevaluation.

The read-only collector now captures time-filtered powerd clamshell decisions,
up to 1,024 entries with truncation and error states, alongside the existing
journal and OS sleep history. Filtering, unavailable/empty distinction and bounds
were checked with synthetic inputs, then collection was exercised on this Mac.
Lid protection stopped after the interruption; do not proceed with an expiry
test assuming it is still active. Full closed-battery expiry, deadline-edge
recovery and active app-update handoff remain open; successful short reconnect
acceptance is blocked by this reproduced enforcement failure.

### New read-only sleep evidence

At 19:57:32 local time the installed build 63 helper recorded macOS beginning
sleep during an active session, with no battery countdown active and no sleep
request by that helper. Unplugging was observed at 19:57:33; a powered-on
notification arrived at 19:57:35, 2.6 seconds after the sleep-begin notification,
followed by the open lid observation. There is no adjacent watchdog sleep command
in the collected journal. `pmset -g log` has display-off/on notifications for this
interval, but no matching Sleep/Wake category record. Notification arrival order
does not establish the exact physical unplug time, cause, or time fully asleep.
The user's activity at that moment has been asked about and remains unconfirmed.
No app replacement, helper restart or sleep request was issued by the agent.

### Desktop test classification corrected

The first new isolated suite was incorrectly run without AGENT MODE. Two tests
explicitly ordered the test Settings window front despite isolated storage and
prohibited app activation. The user caught this interruption. Testing stopped;
the runner now refuses execution without an active session, checks between suites
and immediately before visible panel cases, and supports separate build-only and
run-only phases. AGENTS.md and the reusable settings-flow skill capture this
lesson. Missing and stopped sessions were verified to reject launch. Subsequent
native suite runs were announced and bracketed by the banner.

## Periodic menu glitch — build 67 candidate

The user reports the open main menu glitches every couple of seconds. **P2, reproduced layout defect:** `refreshExternalKeyboardSection` assigns attributed text every periodic refresh; the old `MenuRowView` setter resized to measured text width even when unchanged. AppKit independently stretches all custom rows to the menu width. An isolated native reproduction using the previous source shrank an allocated 620-point row to 430 points on an identical assignment. Variable System/status text can also change preferred widths during tracking. This is a concrete layout defect matching the cadence, not proof that it explains every part of the user's visual symptom.

Build 67 distinguishes preferred from allocated width, ignores equal attributed text, freezes row width during menu tracking, and restores deferred sizing after dismissal. Updated text is truncated within reserved shortcut space and keeps its accessible text. Unchanged external-keyboard title/visibility assignments are guarded. Regression cases replay allocation, unchanged and long updates, shortcut changes, hover retention and dismissal. Light/dark rendering caught and corrected a baseline shift introduced by constrained drawing. No section order, color, overline, spacing or control design was changed.

Final build 67 validation: all 20 isolated suites pass, no compiler warnings, strict bundle/tool signatures verified, and light/dark native menu renders inspected. The old-row reproduction and new allocated-width regression are local AppKit evidence; neither performs a live menu action or hardware change.

The existing pending-keyboard-read state remains explicit; no stale control was made actionable to disguise a refresh. Actual live menu stability and the distinction between resizing versus transient checkbox/label changes remain to be verified after installation. Builds 65 and 66 were intermediate candidates; build 67 includes the earlier two Settings corrections. The settings-flow skill now checks host-allocated geometry during periodic updates, beyond static layout previews.

## Live Settings journey and build 65 corrections — September 8, 2026

AGENT MODE bracketed the live read-only inspection, and was stopped before source work. The user opened the installed build 63 Settings window; no restart or lid-session change was needed.

| Journey | Live result and limits |
| --- | --- |
| Settings → Setup & status → Displays → Monitor inputs → Back | **P2, fixed in build 65:** the initial overview counted the configured display as Ready with “Current input unknown.” Visiting Monitor inputs performed its read, and returning showed Needs attention with the unavailable-input explanation. The overview now distinguishes saved setup from known current input, showing Unverified before that observation. Tests cover unknown, checking, failure, ready and disconnected transitions. Explicit named group destinations retain their existing availability rules. No input was switched. |
| Displays → Switching groups → Back | The empty state explains display membership and input mapping; Back returns to Displays and then the originating overview. No group was added or changed. Physical multi-display operation remains untested. |
| Overview → Keyboards → Navigation keys → Learn/manage layouts → Back | Built-in controls were available; external controls correctly reported no connected external keyboard. **P2, fixed in build 65:** navigation status nevertheless instructed the user to learn keys first. It now distinguishes no device, discovery pending, detection error and missing layout, and preserves saved behavior choices. The nested setup page correctly disabled Start setup and prompted connection/recheck. No keyboard setting or layout was changed. |
| Overview → Scrolling → Review Accessibility → Back | Saved scrolling choices and helper readiness agree. Completed access setup presents a compact Ready state with optional review; returning preserves the page chain. No scrolling or privacy setting was changed. |
| Overview → Maintenance → Back | Repair, helper-file protection and privacy-reset entry points explain their scope and return normally. None was run. |
| Overview → Agent Kill Switch → Agents/shortcut/actions → Back | The regular page exposes named checkbox/popup controls, saved shortcut and autosave/draft semantics. Navigation works through this route. These controls were inspected, not toggled; no physical shortcut, emergency action or permission reset was tested. |

The desktop was returned to Setup & status with AGENT MODE off. Secure password entry, native collector migration, one/two-monitor physical switching and real hotkeys remain acceptance work. The P1 shared lid-control defect remains open. The active installed app/helper set remains build 63; the new candidate is built separately to preserve the user's active lid session. Build 64 was an intermediate layout check: the first explanation measured 61 points in a 59-point row, so build 65 shortens it to 46 points without changing the menu design.

## Build 63 live read-only checks — September 8, 2026, evening

- Authenticated status replies report build 63 for the safety and input helpers. The input helper is active and trusted, ordinary keep-awake and the emergency shortcut report active, and process-event health reports active collection. No hotkey was triggered. All three installed app/helper bundle versions are 63; the privileged helper's new process was verified during installation.
- The user re-enabled lid mode at 19:20:37 local time after the update. Its log records the requested session and accepted command, not verified ongoing prevention. The 23 retained Perch events start at 17:49; macOS's morning clamshell sleep events therefore cannot be correlated with this journal. There is no captured unplug/replug sequence for build 63 in this snapshot. P1 lid reliability remains open. No power state was changed for these checks.
- The event collector still launches `/usr/bin/eslogger` directly. Collection works, but the optional native identity/CPU-accounting migration has not been installed on this Mac. This is distinct from updating the ordinary and lid helpers. Migration/FDA/reboot acceptance remains open.
- Three process CPU-time samples spanning 20 seconds show approximately 0.21 seconds of total CPU time across Perch, both ordinary helpers, the lid helper/watchdog and its existing eslogger (about 1% of one core, with centisecond measurement precision). This is a quiet spot check, not load, latency, leak or whole-app performance acceptance.
- The live UI tool timed out with no Perch window open. AGENT MODE was stopped, and the user was asked to open Settings. No app restart, setting change, OS authorization, password test or new dialog-journey acceptance occurred during this pass. Those checks remain pending.

`Tools/collect-lid-diagnostics.py` now captures a read-only report for the next physical test: bounded Perch history, date-filtered macOS Sleep/Wake/DarkWake entries, current power/clamshell observations and bundle versions. It explains observation limits, records unavailable sources explicitly, never overwrites a report, and does not collect unrelated process arguments, serial numbers or raw assertion logs. The local report captured 23 Perch entries and 12 macOS entries. Date-range/category filtering and unavailable-source preservation were checked. The tool does not verify the kernel prevention mask or solve the lid defect.

## Shared-desktop test handoff — September 8, 2026

The user and agent overlapped live UI interaction during the build 63 update. Chat commentary alone did not make desktop ownership apparent. The developer-only `Tools/agent-mode` utility now presents a high-contrast **AGENT MODE** notice on connected screens, independently of Perch's app process. It uses a nonactivating panel with a **Request control** button, a distinct request marker and an expiring session. Repository instructions require checking immediately before short UI action batches and stopping on requests, expiry, errors or completion.

Validation: the native banner compiled with warnings treated as errors. Six isolated controller cases cover renewal versus status reads, user requests, requests arriving during renewal, expiry, dead/reused processes and session acknowledgment. A live test operated only the banner: its button changed the notice to CONTROL REQUESTED, the next controller check refused continuation, and stopping closed the process. A native screenshot exposed a low-contrast button; explicit dark appearance corrected it and the final render was inspected. Multi-screen/Space placement follows AppKit collection behavior but was not exercised by changing display settings or Spaces. No password focus test was performed in this banner check. The protocol is cooperative and cannot cancel an already-issued command or enforce other agents' tool use.

This does not modify Perch's application sources, menu, settings, helpers or build number. The installed app and ordinary/privileged helpers were verified at build 63 before this work; the lid helper is disarmed after that update. The settings-flow-review skill now includes visible shared-desktop ownership and truthful handoff semantics.

## Build 63 — remaining menu and dialog findings addressed in source

The authorized implementation pass covers DF-01, DF-02, DF-03, DF-04, DF-06, DF-08, DF-09, DF-10, DF-11 and NM-01 through NM-04. DF-05 and DF-07 were already addressed in builds 54 and 52; NM-05's tooltip was corrected in build 57. The original findings below remain as evidence, not a list of still-unfixed implementations.

| Journey | Result | Evidence boundary |
| --- | --- | --- |
| Privacy reset: review → run → leave → return → failure/retry | Operation state belongs to the app session, with separate scope, running and result states. Leaving does not cancel tccutil. Overlapping scopes are refused. Retry cannot be completed by an older callback. Starting another reset first returns to an explicit proposal. | Real page actions with an injected executor; no permission reset. Results survive navigation during the current Perch session, not an app restart. |
| Keyboard settings/details and navigation learning | All four group controls refresh their originating page. Return wording works from either entry route. | Four real button routes with injected writers; no firmware/modifier changes. |
| Group editing: include/remove/add and return | Rebuilding retains scroll distance and identifiable focus. Adding reveals and focuses the new destination; removal offers the nearby destination. Unsaved labels are captured before rebuilding. | Native AppKit fixture; group draft and position assertions. |
| Monitor connection/input-list draft | Model selection fills the draft directly. Back carries a valid input list or a checked connection into the parent draft. One final Save stores the complete setup. Duplicate Cancel and nested Use buttons are removed. Invalid list edits can be corrected or explicitly discarded; an unchanged empty editor can return. A changed connection cannot reuse an earlier check. | Mock monitor, persisted-plan comparisons, failed-save retention, Undo, Back and stale-check cases; no physical switching. |
| Monitor mapping/identification | Saved Mac-input mapping remains visible independently of current readback. Change remains available. Automatic identification can retry after failure, ambiguity or Stop with a fresh cancellation token. Visual confirmation remains explicit. Saving a mapping does not also enable unconfirmed cycling. | Injected probe/confirmation buttons, mock current-input reads, reopening and persistence tests. Physical input identification remains unverified. |
| Shared alerts and permission completion | Embedded informational alerts use shared Back/Close. Action tags and cancellation results are preserved; closing an action-only alert aborts it. Input setup reads the actual input helper and hides grant instructions when ready, with optional review and a separate helper-repair route. Collector readiness hides its drag instructions. | Native alert/action and ready/missing/inactive fixtures, retained focus/handoff tests and light/dark renders. Actual system authorization remains an affected-Mac acceptance check. |
| Non-System menu readiness | Native validation preserves sleep, scrolling and navigation availability. Cold helper reads say Checking instead of Offline; replies repaint consumers without creating another read loop. Scrolling uses the input helper and allows a saved choice to be disabled after access loss. Pending keyboard reads withhold stale Fn choices; recent reads are reused and open-menu refreshes reread external properties. Read-only refresh does not consume pending connection-application work. | Native menu validation, simulated cold/failed helper presentation, authenticated fixture XPC notifications, cached-render/no-poll-loop test, keyboard-mode fixtures and source traces. Physical device changes and performance acceptance remain open. |

Verification for the final build is recorded in RELEASE-1.2.md. The tuned menu grouping, section appearance, colors and System area are unchanged. The settings-flow-review skill now explicitly checks safe Back behavior after removing child Apply and prevents status rendering from creating an unbounded request loop.

Still open: the P1 shared macOS lid-control flag defect; actual 60-second undocking and failure behavior; affected-Mac secure password entry, controls and hotkeys; real one/two-monitor switching and external-input changes; native collector migration/FDA/reboot acceptance; broader performance/accessibility/recovery acceptance and clean-install/distribution/Homebrew work. This batch does not establish release readiness or repair the shared clamshell flag.

## BW-01 implementation addressed — build 61

Removed the deprecated launchd job lookup instead of suppressing it. `PerchEventLauncher` is a fixed-command, startup-only native executable: it writes an atomic root-owned identity record and execs `/usr/bin/eslogger fork exec exit`. The process PID and birth remain the same across exec. App reads are descriptor-relative and bounded; ownership, writable modes, link/special-file attacks, boot mismatch and process liveness/birth are validated. CPU sampling compares collector identity before and after its scan and marks totals incomplete across transitions. Missing identity on an existing/legacy installation means unavailable, never zero collector usage.

The explicit collector-page update installs the separately signed launcher. Existing direct-eslogger jobs remain valid event collectors and are not silently upgraded or falsely marked broken. The installer verifies a staged launcher against the app's signing identity with the dedicated launcher identifier; it stages/lints the fixed job before bootout. A failed diagnostic record write still execs the collector. No new persistent root process or Endpoint Security entitlement is introduced.

Isolated native fixtures exercise the actual launcher with a compile-time test executable and a directory owned by the test user, verifying same PID/birth across exec, restart replacement, fixed arguments/environment, unsafe-directory rejection, symlink replacement, metadata failure continuation and failed exec. Swift fixtures cover stale/reused identity and unsafe file reads. These tests do not execute eslogger or install a launch job. Native launcher Full Disk Access, installed launchd restart/reboot behavior, and live affected-Mac CPU attribution remain acceptance work. Prior shell-launcher permission failures make that permission check material; no existing grant is claimed to suffice without event delivery.

Validation: production and isolated test builds completed with zero compiler warnings. All 20 isolated suites and the native launcher fixture checks passed. App, display-tool and launcher strict signatures passed; the installer’s derived launcher requirement matched, and the production launcher refused unprivileged execution. The isolated harness now replaces blocked power-method bodies instead of retaining unreachable code, preserving the safety blocks without their two synthetic warnings. No warning suppression was added.

## System-dialog sibling audit — build 60

The requested [complete focus inventory](V1.2-FOCUS-REVIEW.md) found and addressed missing busy ownership in native picker/standalone alert paths, competing permission-prompt/Settings launches, and floating panels/delayed notices during external-app handoffs. It identifies evidence separately for every administrator route, all three picker entries, all 16 alert sites, nine external destinations and activation paths. Shared page/result presentation now respects the whole interaction lifetime. The review skill was refined to require native fallback branches and external-app return semantics as part of sibling sweeps.

The original affected-Mac password-entry failure remains pending acceptance. This focused audit does not close unrelated UX findings, the shared lid-control defect, or hardware acceptance.

Validation: the final production build and all 20 isolated suites passed, including the new picker-entry and external-link action tests. Strict app/helper signatures and archive integrity were checked before packaging completion. One distinct production warning remains (BW-01). No live OS grant, password entry, installation, panic or hardware operation was performed.

## P1 — Administrator password prompt rejects typing on first attempt — build 59 candidate

The user reproduced this on the other Mac's new build when enabling “Including with the lid closed”: the OS password dialog accepted no typing, but cancelling the OS dialog and trying again succeeded. Exact app/helper build numbers and key-window ownership on that machine were not captured. This is separate from early lid sleep.

Source inspection found `beginAuthorization()` lowered Settings without withdrawing its key floating panel and immediately requested app activation before synchronous AppleScript authorization. Apple's [activation documentation](https://developer.apple.com/documentation/appkit/nsapplication/activate(ignoringotherapps:)) warns activation can occur later. This is a plausible focus race, not a reproduction of the OS symptom. Two delayed app-result paths (generic errors and shortcut-test success) also lacked authorization guards.

Build 59 withdraws the panel without closing or reconstructing its page, removes the competing activation, restores only an originally visible Settings page, and defers those result paths. The shared wrapper covers lid installation/cleanup, event setup, privileged helper removal and legacy sleep commands; none was executed live. Existing startup/permission/critical-issue notices already recheck authorization before showing. Nested and duplicate completion retain the handoff until the final operation ends. The settings review skill now explicitly requires first-attempt/retry focus checks and tracing queued activation/result UI; a window-level assertion alone cannot close this report.

**Acceptance remains open:** first password entry, cancellation/retry and return to the same page on the affected Mac, including its actual local/remote input method. No credentials should be inspected, injected or logged. Source fixes and isolated panel tests do not prove that secure OS text input works there.

Validation: production compilation and all 20 isolated suites passed. Native panel checks cover withdrawal, page retention, refresh suppression, deferred notices, nested/duplicate completion, immediate retry and menu-only entry. No real OS authorization dialog was opened. The previously tracked BW-01 compiler warning remains.

## Build warnings reviewed — build 58, 2026-09-08

Build 57's six warning lines were three compiler diagnostics repeated in source excerpts. The two unsupported Audio Hardware Service functions were replaced with current AudioObject property access; the virtual main-volume selector and unavailable-state handling are preserved. Read-only comparison on this Mac matched all three enumerated devices, including unsupported controls. These deprecations do not establish a cause for the reported sleep incident.

- **BW-01 · P2 — Event collector CPU attribution uses deprecated job lookup. Addressed in build 61 source; installation acceptance pending.** `ProcessCPUReader.read()` uses `SMJobCopyDictionary` to identify the exact `local.scott.perch.events` launchd job PID. The current Apple SDK states the function will be removed and has no provided replacement. `SMAppService.status` does not expose a running PID. Keep the warning visible pending a reliable collector-identity/status path, validated against unrelated `eslogger` processes, collector restart/PID reuse and unavailable identity. The latter must produce incomplete/unavailable accounting rather than confidently undercount Perch. This is a compatibility debt, not a reproduced current CPU-reading failure; it does not justify changing live services during this review.

Sources: [Apple's job lookup documentation](https://developer.apple.com/documentation/servicemanagement/smjobcopydictionary(_:_:)), current SDK `ServiceManagement.h` and `SMAppService.h`, and [AudioObject property reads](https://developer.apple.com/documentation/coreaudio/audioobjectgetpropertydata(_:_:_:_:_:_:)).

Validation: production compilation and strict app/helper signatures passed, with one remaining distinct compiler warning (BW-01). All 20 isolated regression suites passed. The isolated test copy also emits two unreachable-code diagnostics from its deliberately injected power-operation blockers; those are not production warnings. Prepared separately from the installed app, without live permission, helper or hardware changes.

## Confirmed lid-client defects addressed — build 57, 2026-09-08

Following the user's direction to fix confirmed defects before hardware testing, lid renewal now uses a dedicated serial dispatch queue/timer and an active-session responsiveness activity. The menu app's main thread no longer owns heartbeat execution. A transport/reply failure drops displayed certainty and invalidates the connection while preserving the existing token for retry, bounded conservatively by five seconds from the last acknowledged request's send time. Only explicit user enable can create a session. The helper's original expiry/authorization checks remain authoritative; the watchdog deadlines and unknown-sensor fail-safe are unchanged. Results are consumed exactly once, so late replies/errors cannot undo recovered state or subsequent user changes.

macOS sleep beginning during an armed session now stops its policy and leaves a persistent diagnostic through wake; the next supervised tick releases the command without adding a policy sleep request. The diagnostic attributes only what this helper knows and directs attention to earlier watchdog entries; it does not infer why macOS slept. Active-session copy and the overview's new Unverified state distinguish command acceptance from continued effective protection. This addresses misleading presentation and post-sleep state, **not the underlying P1 shared kernel-flag interference**. No periodic rewrite of the shared flag was added because the kernel's immediate sleep reevaluation would still race it. That mechanism remains open and requires a separate correction and physical validation.

Validation: all 20 isolated suites passed, including recovery, late replies, expiry, explicit disable and a real client heartbeat running while the UI thread was blocked. After shortening the overview status text, the lid and overview suites passed again. Native light/dark overview renders, production compilation and strict app/helper signatures passed. All sleep and enforcement mutations were injected; the build was prepared without installing it or changing live helpers, permissions or hardware settings.

## Selected menu title treatment — build 55, 2026-09-08

The user selected option B, overline plus title highlight. The native renderer now draws a two-point colored overline and faint background only on non-System section titles. System stays neutral and first; Agent Kill Switch stays second. Three-point gaps, row heights, menu actions, enabled-state handling and shortcut labels are preserved. All 20 isolated suites pass; native light/dark menu renders, production compilation, strict signatures and archive integrity were checked. The build is prepared separately from the installed app. The monitor-contrast diagnosis below closes only the disappearing-control appearance report; early lid sleep and other functional findings remain open.

## P1 — Reported sleep during the undocking grace interval — 2026-09-08

The user enabled closed-lid Keep awake on the other test Mac, closed the lid, unplugged, and restored power after approximately five seconds. They encountered the password screen and a log entry reporting wake completed 29.7 seconds after sleep began. The affected Mac's complete log and exact app/helper versions have not been supplied. This Mac's older installed binaries or power records cannot establish the cause on that Mac.

Keep awake and lid changes apply immediately through their asynchronous operation; no Save or closing step commits them. The lid UI must finish enabling before protection can be relied on. The normal policy must not request sleep for a five-second battery interval followed by external power. The wake entry pairs macOS will-sleep and powered-on notifications using a continuous clock: it reports the interval between notifications, including transitions, not exact time asleep or a 29.7-second grace period. A password screen alone would not establish sleep; the OS notifications provide the additional evidence.

**Source-confirmed enforcement gap; incident causality unconfirmed:** `LidGuardEnforcer` remembers a successful command in its local `preventing` Boolean and does not verify continued effective prevention while that Boolean remains true. Perch calls `kPMSetClamshellSleepState`, which XNU maps to the shared `kClamshellSleepDisablePowerd` reason bit. Apple's power manager also writes this interface as desktop mode/assertion conditions change. It can therefore clear the bit while Perch continues reporting an armed policy and plausible countdown. This promotes the previously recorded powerd-interference acceptance risk to an explicit P1 investigation; it is not evidence that the 60-second policy itself expired early. Sources: [XNU user-client dispatch](https://github.com/apple-oss-distributions/xnu/blob/main/iokit/Kernel/RootDomainUserClient.cpp), [powerd clamshell-state calculation](https://github.com/apple-oss-distributions/PowerManagement/blob/main/pmconfigd/PMAssertions.c).

Next evidence: copy the affected Mac's Lid activity log around enabling, unplugging, reconnecting, sleep beginning and wake. Distinguish a Perch sleep request or watchdog/lease/sensor failure from an OS-initiated transition; obtain that Mac's read-only macOS sleep-reason records if needed. Verification must include actual closed-lid undock/reconnect on the affected hardware, with the user operating it. Passing mock policy tests or accepting the kernel command cannot close this finding. No live power control, helper restart, permission reset or product change was performed for this investigation.

### Static follow-up and isolated replay — build 55 sources

- **P1, reproduced enforcement defect:** A harness using unchanged copies of `LidGuardPolicy`, `LidGuardEnforcer` and `LidGuardWatchdogState`, with fake hardware and simulated time, enabled protection, injected an external clear, then replayed unplug at 1s / reconnect at 6s / continued AC to 100s. Perch still reported `preventing == true`, actual fake prevention remained false, and the only hardware write was the original enable. There were no Perch sleep requests. A control replay without the external clear retained protection and never requested sleep. This confirms the implementation cannot detect or recover from this interference; it does not establish which writer acted on the user's Mac.
- **Why a periodic rewrite is not a proven fix:** Apple's kernel immediately reevaluates closed-lid sleep when the last prevention bit is cleared. In eligible conditions, that evaluation calls `privateSleepSystem(kIOPMSleepReasonClamshell)` before Perch's next polling opportunity. Merely writing the bit again every second still races that transition and can conflict with another owner's cleanup. Acceptance must establish the protection mechanism remains effective through the power/dock transition, not merely that a retry was accepted afterward.
- **Early fail-safe paths, reproduced as designed:** With no further supervisor frames, a watchdog lease expiring at 3s requested sleep only 2s after unplug. A single `.unknown` power observation with the lid closed also immediately released protection and requested sleep; later AC did not re-arm the stopped session. The supervisor separately requires watchdog acknowledgments younger than 2s and app lease renewal before its 5s expiry. These are independent of the 60s normal policy, and require their own log evidence before attributing the incident to a failure deadline.
- **P2, source-confirmed renewal fragility:** `LidGuardClient.refresh()` sends renewal/status requests from the menu app's main run loop. A single 2s reply timeout clears the renewal token; subsequent refreshes become status queries and cannot extend the still-running session. A transient delayed reply can therefore lead to lease expiry even if the app is otherwise healthy. Investigate bounded recovery within the existing lease and an appropriate execution context, preserving the fail-safe rather than extending deadlines blindly. App Nap, a main-thread stall or any particular timeout on the affected Mac has not been observed.
- **Log interpretation:** A wake transition lasting 29.7s can itself outlast the helper leases. Failure messages on wake may consequently be effects of the earlier sleep, not its initiating cause. Use the entries before the first macOS sleep-begin notification to determine causality.

Apple source was inspected at fixed revisions: [powerd desktop-mode eligibility](https://github.com/apple-oss-distributions/PowerManagement/blob/d415e45501842834a280930c3eed9186544a67f0/pmconfigd/PMDisplay.m#L201), [powerd shared-bit updates](https://github.com/apple-oss-distributions/PowerManagement/blob/d415e45501842834a280930c3eed9186544a67f0/pmconfigd/PMAssertions.c#L1389), [XNU interface dispatch](https://github.com/apple-oss-distributions/xnu/blob/f6217f891ac0bb64f3d375211650a4c1ff8ca1ea/iokit/Kernel/RootDomainUserClient.cpp#L525), and [immediate kernel reevaluation](https://github.com/apple-oss-distributions/xnu/blob/f6217f891ac0bb64f3d375211650a4c1ff8ca1ea/iokit/Kernel/IOPMrootDomain.cpp#L4541). Published source is evidence of the mechanism, not verification of the affected Mac's exact running OS implementation. All replay mutations were injected; no live power, permission or hardware state was changed. No implementation fix was made in this follow-up.

## Agent settings interaction and shortcut labels — build 54, 2026-09-08

The user reports the Agent Kill Switch editor on the other test Mac accepts scrolling but not control interaction. The previous editor transfers an NSAlert accessory into the shared settings panel and runs a nested modal session. An isolated local hit test could reach and toggle its checkbox, so the exact affected-Mac failure is **not reproduced**. The prior regression assigned checkbox states directly and did not establish user interaction.

The replacement is a regular nonmodal page with explicit native control actions, immediate agent/scope saving, validated shortcut saving, retained working settings on invalid/conflicting edits, failed-save retry, and ordinary Back navigation. Changes merge into freshly loaded configuration. DF-05 is addressed in source; the user's affected-Mac click and rendering reports need retesting there. Other shared modal dialogs remain a separate review item (DF-06); this change does not establish that their interactions are sound.

Panic and monitor-cycle rows now display configured enabled shortcuts at the right edge, including the selected display group's shortcut. Menu status still communicates offline/checking/unavailable separately. These are presentation labels, not new native key equivalents, so global hotkey dispatch is unchanged. The review skill now requires hit testing/action dispatch/persistence coverage and distinguishes it from directly assigning model or checkbox state.

## Menu placement and remote appearance follow-up — 2026-09-08

At the user's request, the entire red Agent Kill Switch section moves directly below System in build 53. Panic, the system-wide privacy reset and conditional Resume stay together. This is an explicit update to the protected main-menu layout; the other sections retain their relative order. Stronger outlines are still a preview, not an implemented styling change. Supply inline PNG images for appearance iterations because the user cannot view interactive previews on their phone.

The user reported that unchecked boxes, idle buttons and list-box borders disappeared on the other test Mac. **Resolved by user diagnosis on 2026-09-08: the affected monitor's contrast caused the missing control outlines.** This report is not an established Perch rendering defect. The title-style experiment is a separate design choice; it is not the fix for that monitor condition. The earlier agent-control interaction report remains separate.

## Reusable settings review method

For settings/setup UX work, use the [settings-flow-review skill](Tools/skills/settings-flow-review/SKILL.md), also installed as the personal `$settings-flow-review` skill. Its versioned source lives here and is general-purpose; Perch-specific decisions remain in this review. Inventory the whole in-scope dialog set, trace the applicable first-use, ready, change, repair, failure/retry and reopen journeys, and sweep sibling pages whenever a pattern is found. Continue authorized fixes and verification within the same task instead of waiting for the user to identify each omission.

The skill explicitly updates its review method when a user correction or demonstrated missed pattern warrants it. Preserve exceptions and existing product decisions rather than accumulating absolute rules. Such maintenance does not mark Perch's open findings resolved; implementation and physical acceptance still need their own evidence.

## Non-System menu first-display audit — build 52, 2026-09-08

The earlier first-display checks covered monitor controls, not every menu option. The new [complete menu state audit](V1.2-MENU-REVIEW.md) inventories all **21 non-System action rows**, including conditional keyboard and Resume actions, and traces opening, pending work, validation, completion, repeated opening and external changes. System metrics are excluded. All rows received source inspection; targeted isolated AppKit cases reproduced the issues noted below. No physical menu operation was activated.

- **NM-01 · P2 — Sleep controls are re-enabled by native validation.** `applyLidSleepPresentation()` disables the lid row when Keep awake is off and both rows when state is unknown; `validateMenuItem()` returns true. The isolated real menu's `update()` changed disabled controls to enabled/accessibility-enabled. Share the readiness rule across presentation, native validation and activation. Handler guards still exist; no unsafe power operation was demonstrated.
- **NM-02 · P2 — Keyboard state can remain stale initially and while open.** External Fn retains an old checked/enabled mode during an ordinary pending read. Periodic menu refresh redraws cached modifier metadata and external Fn results without rereading those properties; changes lacking a relevant notification can remain stale. Pending registration can also retain old external names/visibility. The pending Fn state was reproduced; actual firmware/modifier changes were not made.
- **NM-03 · P2 — Pending helper reads appear offline and replies do not repaint immediately.** A cold asynchronous cache produces Watcher offline/Repair needed and hides Resume before a reply establishes status. The reply updates the cache without notifying the menu; the isolated row stayed offline until another refresh. Input and lid status have related delayed-presentation paths. Distinguish initially checking from failed/stale and notify consumers on relevant results.
- **NM-04 · P2 — Scrolling readiness uses the wrong dependency and omits inactive controls.** Healthy input with an absent guardian routes to permission setup; trusted but inactive input shows normal checked Vertical controls. Both cases were reproduced with injected status. Use the actual input helper and separate saved choices, permission and running behavior; preserve disabling a saved choice after lost access.
- **NM-05 · P3 — Lid tooltip still requires an open lid.** `main.swift:363` contradicts build 47's support for setup while closed on confirmed external power. Update the menu's secondary copy along with feature-page copy.

NM-01 through NM-04 are addressed in build 63 source and isolated checks; NM-05 was addressed in build 57. Physical acceptance remains open. Audio and login use fresh native reads at opening/normal refresh; stateless display-off, privacy-reset entry, About and Quit have no additional first-display dependency defect identified. Existing dialog defects and physical acceptance remain separate. The copied audit executable stubs helper replies and keyboard worker execution, retains the isolated mutation blocks, skips System refresh, and never activates menu actions. No product implementation, live permission, panic, hardware setting, installed helper or app build changed. The review skill now explicitly audits every non-excluded menu row and its complete presentation/validation/update path.

## Remaining dialog-flow findings — build 51, 2026-09-08

These findings were recorded before fixes. DF-05 was addressed in build 54, DF-07 in build 52, and the remaining listed implementations in build 63. See the current implementation/evidence ledger above; hardware and OS acceptance remain separate. The audit considered entry, changing choices, completing setup, Back/Close, returning from child pages, and failure recovery. Removing the literal Done label did not complete the agreed flow standard.

1. **DF-01 · P1 — Privacy reset cancellation is misrepresented.** `ResetActions.swift:44–58` continues to say “Back cancels” after launching the reset. Leaving only suppresses its callback; macOS continues the operation. Use distinct pre-action/running/result states, preserve completion, and provide recovery or retry after failure instead of leaving the action permanently disabled. No live reset was tested.
2. **DF-02 · P2 — Keyboard details changes push another settings page.** Both function-key and modifier handlers call `keyboardSettings()` regardless of the originating page (`KeyboardSettingsUI.swift:124,134`). An isolated action with its physical writer stubbed produced `Perch settings → Keyboard settings → Keyboard details → Keyboard settings`. Refresh the originating page in place.
3. **DF-03 · P2 — Group editing loses the user's position.** Including/removing displays or adding/removing destinations rebuilds the view (`MonitorGroupsUI.swift:139,150,164`); `SettingsWindow.swift:100` then scrolls it to the top. The isolated Add destination flow jumped from bottom offset 0 to top offset 460. Preserve position and reveal/focus newly added fields.
4. **DF-04 · P2 — Monitor setup retains nested acceptance and duplicate Cancel.** Exact-model setup requires selecting a model, Use preset, Use this input list, then outer Save. Connection setup requires Check, Use this connection, then Save. Compatibility confirmation adds another layer. The outer editor has both header and footer Cancel. Use one coherent draft with predictable Back navigation and at most one justified final transaction boundary; ordinary selections should not need separate Use buttons (`MonitorInputUI.swift:140,332,343,456,631,674`).
5. **DF-05 · P2 — Agent settings deferred ordinary choices in a combined Save/Cancel form. Addressed in build 54 source.** The editor now uses a regular settings page. Agent choices and privacy-reset scope save independently; shortcut edits save when valid, preserve the working configuration when incomplete or conflicting, and offer retry after failed writes. Back/reopen shows saved choices. The affected test Mac still needs user-operated acceptance.
6. **DF-06 · P2 — Shared alerts retain redundant exits.** `SettingsWindow.swift:153–170` copies alert buttons alongside shared Back/Close. Shortcut results duplicate navigation with a footer Back; informational/error results also have OK; confirmations duplicate cancellation. Keep one navigation exit plus any justified, explicitly named action. The isolated result showed shared Close and footer Back.
7. **DF-07 · P2 — Overview monitor readiness differs during discovery debounce. Fixed in build 52 source.** The overview used `busy`, not build 51's full `checkingDisplays` state; a group could still be assessed from its old member snapshot. It now includes discovery and group work in pending readiness, and cannot report an available group from that pending snapshot. Isolated AppKit regression checks cover individual and group debounce; the other Mac remains physical acceptance work.
8. **DF-09 · P2 — Monitor mapping completion depends on the wrong state.** `MonitorInputUI.swift:191–227,459,485–495` shows Identify this Mac's input when current-input readback fails, even after `macInput` was saved; a successful read hides the action even without a saved Mac mapping. Saving offers only a transient success message. Show the saved mapping durably with optional Change, separately from observed current input and Check current input. Unknown current input must not ask the user to repeat saved mapping setup, and a saved mapping must never establish the cycle position. Retain named destination switching and current-input confirmation where needed.
9. **DF-10 · P2 — Permission setup does not fully transition to ready.** `PermissionSetup.swift:17–48` only updates its status text/color after verified readiness; numbered grant instructions and drag/enable controls remain prominent. Show the ready result and optional permission review, restoring the specific repair instructions only when needed. Process-event setup already hides its primary setup action when ready, but its fixed permission controls and setup description should become secondary too (`EventSetup.swift:58–75,106–108`).
10. **DF-11 · P2 — Automatic monitor identification has no in-page retry.** `MonitorInputUI.swift:246–262` disables the automatic probe button and never re-enables it on completion, stopping or failure. Manual testing remains available, but repeating the automatic check requires Back and reopening. Provide an appropriate Retry action with a fresh cancellation token; a completed saved mapping should show DF-09's ready state.
11. **DF-08 · P3 — Return wording and setup copy need cleanup.** Navigation learning promises a return to Navigation keys even when opened from Keyboard details; group instructions refer to Back while the button says Cancel; process-event instructions still mention Codex. Make wording accurate for every entry route.

The completion-state follow-up found the initial assessment too generous about monitor identification and Accessibility setup. Apply the same standard as recognized keyboards to every setup page: show what is saved and what is currently working, request only the missing step, make Change/Recheck optional when ready, and offer a specific recovery when state changes. Keep saved configuration separate from current evidence. Ordinary preferences save immediately; explicit tests, visual confirmation, validated related-field drafts and destructive operations retain a clearly named action where justified. Back/Close must not require another completion action. DF-09 through DF-11 were source-confirmed here and subsequently addressed in build 63; physical acceptance remains open.

The source pass covered Settings home/overview; all display, connection, input-list, compatibility, identification and group pages; keyboard/details/navigation/learning/exceptions/reset; Accessibility and process-event setup states; scrolling, awake and lid history; agent configuration/management/catalog/preview/test/confirmation; maintenance/install results; privacy/system/preference resets; App settings, About and errors. Existing automatic keyboard completion and app-exception saving remain consistent. Explicit confirmation remains appropriate for destructive operations, installing software, importing a selected file, and confirming the visible result of a monitor test.

Verification: a focused AppKit audit copy built from the existing isolated build-51 sources reproduced the monitor controls, shared exits, agent draft, keyboard navigation stack and group scroll jump. Monitor requests, real mode writes, privacy resets and panic actions were all zero. Cancellation semantics and the overview mismatch were traced in source. No implementation fixes, app/helper replacement, permission changes, hardware changes or release publication were performed. These new findings are not covered merely by the prior 20-suite pass.

## Monitor readiness before first show — build 52, 2026-09-08

**P2 functional finding:** Build 51 started full metadata discovery on every menu opening, even when startup or the last display change had already established readiness. That guaranteed a disabled first frame and did not meet the user's request for an accurate initial menu. This source defect does not establish why a control might stay disabled indefinitely on a different or older running build; this Mac was still running build 47 during the correction.

The menu now keeps the result from background discovery while a cheap macOS online-display identity snapshot is unchanged. Startup, screen changes and wake populate readiness before opening. New, unknown or unreadable topology still requires checking; comparing before and after the adapter request also rejects a change that precedes its AppKit notification. A known missing monitor opens directly to its Settings route. Failed discovery retries on opening, whereas an unchanged healthy menu no longer launches another adapter request or becomes busy. Existing native/accessibility validation, generation checks, coalescing and explicit fresh current-input reads before cycling remain. The overview includes the same pending state for individual monitors and groups (DF-07).

Verification: all 20 isolated suites passed. The expanded monitor flow covers the first ready frame, repeated known/absent openings, background wake refresh, missed/delayed screen notifications, changed identity with the same display ID, failure/retry, unreadable topology, queued operations and overview/group agreement. Fake adapters allow only metadata requests. A separate read-only sample of the production macOS identity function returned the online display and measured about 29 ms for its first call, with a fastest subsequent sample of 0.08 ms; no input readback or switching was performed. The candidate is built separately; no live permissions, panic, sleep settings or installed helpers were changed.

## Monitor menu readiness correction — build 51, 2026-09-08

**P2 functional finding:** Cycle monitor input could appear enabled before discovery established whether the saved monitor was available. Opening the menu only repainted cached metadata; screen-change debounce retained old readiness, and native menu validation could re-enable the item during a request. This made an unavailable control look usable on another Mac.

The menu now starts a read-only metadata check before its first draw and disables the row with “Checking monitors…” until that request settles. Startup is unknown by default. Screen changes invalidate readiness immediately; superseded replies cannot restore an old route, overlapping menu opens share a discovery request, and queued discovery does not publish a usable gap. Failed discovery clears old DDC routes, and successful recovery clears the discovery error. Native validation and accessible activation use the same readiness rule. Missing setup keeps its Settings route; active groups account for missing members, while a saved independent USB/LAN route remains available when its video input is inactive.

This checks connected-display/control metadata, not the monitor's current input or physical command success. Cycling still reads current input when explicitly invoked. No monitor switch, input readback, live permission reset, panic or power-setting change is performed by menu opening or by the mock regression checks.

Build 51 regression evidence: all 20 isolated suites passed, including first draw, reopening, native and accessible validation, menu-tracking completion, debounce, stale replies, errors/recovery, queued checks, group readiness and independent control routes. Physical acceptance on the other Mac remains open.

## Agreed main menu scope

Preserve the care and clarity of the tuned main menu. On 2026-09-08 the user explicitly requested an experiment: restore System to the top with a neutral background, tint the other groups with their existing colors, remove separators and keep spacing smaller. Build 46 uses three-point gaps. This supersedes the earlier ordering of system metrics after common tasks. Retain reduced repeated warnings and clear Quit behavior.

**Keep all-app privacy reset in the main menu.** It exclusively changes system permissions; it is not a Perch preference. The proposal to move it into Perch Settings is rejected. The accepted hierarchy and wording refinements do not authorize removing or hiding that system action. The multi-monitor and lid-grace requirements do not imply new menu controls. Functional repairs to menu actions, state reporting, setup links, and authorization handoffs remain first priority.

## Implementation priority

**Functional bugs must be fixed first, including bugs behind main-menu controls.** The user explicitly clarified this priority. Address the existing correctness, safety, reliability, data-loss, and broken-flow findings before discretionary UX redesign or new features, with the highest-severity defects first. Preserve the main menu's tuned design while making its behavior correct. A clipped approval, lost draft, misleading state, or broken navigation is a functional defect even when it appears in a dialog. Earlier wording that deferred functional bugs to separate later work is superseded. Retain the existing limits on live permissions, panic, hardware changes, and publication, and distinguish safe automated verification from remaining user-operated hardware/OS acceptance.

## Reported bugs to fix in 1.2

These reports include the original 1.1 failures and issues reported during the 1.2 preview. The implementation checkpoint below distinguishes source fixes from hardware/OS acceptance still required; automated suites do not disprove user reports.

- **P2 — Keyboard learning still has redundant acceptance/cancellation controls.** The 2026-09-08 correction explicitly supersedes the earlier preview/accept rule for navigation learning: completing all four keys saves the layout automatically, after the final release or absent-key answer. There is no Done, Use this layout or Cancel setup action; Back leaves completed setup or stops unfinished setup while retaining the previous layout. An unsuccessful save preserves the old layout and offers a clearly labeled Retry saving action without starting another lesson. Navigation app-exception checkboxes also save immediately and use Back; failures restore the previously saved checkbox state. Known layouts remain automatic. Other multi-field monitor/agent editors still use their documented draft transactions; no claim is made that all remaining dialog journeys have passed user acceptance.

  Build 50 verification: all 20 isolated suites pass, including completion/reopen, partial replacement, Back/close, timeout/focus loss, save failure/retry, absent keys and immediate exception choices. Light/dark completion views show the saved result with Back as the sole page action. The source scan finds no remaining Done, Use this layout, Cancel setup or Save exceptions controls. Hardware input and live settings were not changed for these tests.

- **P2 — Known keyboards incorrectly require navigation setup on a new Mac.** Reported 2026-09-08: MX Keys Home/End/Page Up/Page Down should be recognized without teaching four standard keys. Perch already bundles 28 profiles, including this exact MX Keys identity and transport. A read-only reproduction returned no physical keyboards through `IOHIDDeviceConformsTo`/element queries while registry metadata contained the keyboard and all four usages. Registration now reads bounded registry metadata without creating an HID input client; raw input access is reserved for explicit learning. Exact model/transport/descriptor matching and saved overrides remain. The learning page also showed “not identified yet” for recognized layouts; it now shows the available layout and explains that learning a replacement is optional. Fresh-preferences, unreadable/malformed descriptor, internal/virtual device, and optional-learning regressions are included. Physical key delivery and a separate clean Mac remain acceptance checks. [Apple conformance implementation](https://github.com/apple-oss-distributions/IOKitUser/blob/main/hid.subproj/IOHIDDevice.c).

  Build 48 verification: all 20 isolated suites, production compilation and strict signature verification passed. The production metadata/registration code recognized the live MX Keys with no saved profiles; no input client, learning session, permission mutation or key injection was used for this check.

- **Remote lid setup — build 47:** The open-lid startup requirement was a Perch precaution, not an Apple installation restriction. The user authorized removing it for confirmed powered operation. New sessions accept a known closed lid on external power, recheck after watchdog acknowledgment, and reject a closed lid on battery or unknown sensor state. An existing session retains the 60-second undocking interval; restarts still begin disarmed. Setup/recovery text now matches this behavior.

- **P1 — Monitor identification leaves the user without a clear next action.** Reported 2026-09-08: “You confirmed USB-C · use within 30 seconds” is confusing. The flow conflated saving this Mac's port with establishing a temporary cycle starting point. Build 46 separates those results, names the Save action and explains direct destination switching. It adds the requested explicit experiment over the selected monitor's known inputs, observing that monitor's macOS connection through extended or mirrored use. A reconnect only suggests a candidate; persistent connectivity or current-input readback cannot alone prove which computer owns the port. Stop/Back cancels and attempts return, with physical-button recovery if control is lost. Simulated tests cover scope, ambiguity, cancellation and return. The physical experiment has not been run and the broader monitor journey remains open.

- **Lid activity acceptance evidence:** Build 46 adds one persistent local history capped at 24 hours and 1,024 entries, with helper/watchdog actions, elapsed lid/power transitions, countdown reasons, command outcomes and independent macOS sleep/wake notifications. The UI supports live updates, pause, Copy log, Back and offline history. Storage and enforcement have separate execution queues; pruning runs on changes and once a minute while the helper is running. Absence of an event is not proof that a transition did not happen during an observation gap. Physical undocking/sleep and crash-recovery checks remain required.

- **P1 — Build 44 falsely rejects lid control using cached state.** Reported 2026-09-08: “macOS did not confirm lid-sleep prevention. This mode is unavailable on this Mac.” All installed executable hashes matched build 44; the command reached its success return and was rejected by Perch's own postcondition. XNU's `setClamShellSleepDisable` changes the internal mask without refreshing `AppleClamshellCausesSleep`; that property is only refreshed when a clamshell notification is sent. Build 45 removes this invalid readback check and its use as session authorization, retaining command errors and independent deadline/watchdog enforcement. A cleaned-up failed start reports off with its error, so ordinary Keep awake remains usable. “Enabled” describes the accepted command and live supervisor, not proof of physical sleep behavior. Actual undocking/sleep and interference with powerd's shared reason bit still require acceptance. [Apple setter and notification implementation](https://github.com/apple-oss-distributions/xnu/blob/main/iokit/Kernel/IOPMrootDomain.cpp).

  Build 45 verification: production build and all 20 isolated suites passed. After updating the app and all helpers, the live Settings enable → disable → enable flow succeeded with the lid open on AC. Enabled status persisted beyond the lease intervals, disabling removed the session record, and ordinary Keep awake/input access remained active. Physical closed-lid behavior remains untested.

- **P1 — Build 43 lid cleanup fails before connecting to power control; fixed in build 44.** Reported 2026-09-08: “⚠ Sleep: Lid cleanup failed: macOS power control is unavailable.” `IOPMFindPowerManagement` was incorrectly given `mach_task_self_` in both the lid override and sleep-request paths. Apple's documented argument is the default IOKit port; a read-only open/close check on the affected Mac reproduced failure with the old argument and success with the corrected argument. Build 44 shares the corrected connection setup, adds a real connection regression without power mutations, and upgrades an older installed helper before attempting cleanup through it. Earlier injected-hardware suites missed this connection error. All 20 suites passed; the live app and helpers were upgraded, and the normal lid-helper repair successfully removed the stale session and restarted the build 44 privileged service/watchdog disarmed. Actual closed-lid sleep and failure acceptance remain open. [Apple API contract](https://developer.apple.com/documentation/iokit/1557133-iopmfindpowermanagement).

- **Settings guidance must have one consistent interaction.** Any item saying “See Settings,” “Set up in Settings,” or similar must take the user directly to the relevant settings page when clicked. Use this behavior consistently across the app, including Cycle monitor input and every permission/setup/repair hint. While prerequisites are missing, clicking must perform navigation instead of attempting the unavailable operation. Audit every such label and verify mouse and keyboard activation, the correct destination, and Back/Done navigation. Avoid enabled items that do nothing or merely repeat the warning.
- **First lid-sleep authorization dialog still cannot accept typing.** On the first attempt to change Including with lid closed, the administrator password dialog appears but its input cannot be typed into. Previous menu-dismissal changes did not resolve the user's report. Investigate menu tracking, application activation, settings-window level/focus, and authorization presentation; do not assume the cause is established. Acceptance: the first prompt accepts normal user typing, cancellation preserves accurate queried state, and subsequent attempts work too. Cover both the lid control and master Keep awake paths that require authorization. Verify with a user-operated prompt; never capture, inject, or log a password, bypass authorization, or change live sleep settings as an automated test. Mock success/cancellation tests alone cannot close this bug.
- **P1 — Switching from another Perch-enabled computer leaves monitor state stale.** Reported 2026-09-07: when two computers share a monitor and each runs Perch, a switch performed by one computer can leave the other believing its previously selected input is still active. Its next cycle can therefore choose the wrong destination or appear to do nothing. Reproduce with computers A and B: switch through Perch on A, change the monitor input through Perch on B, then inspect A's displayed state and next-switch decision; repeat in the other direction and after a monitor-button change. Perch needs a discoverable way to check which input the monitor is actually displaying, and must reconcile external changes before deciding the next input. Distinguish a fresh monitor report, a user-confirmed observation, and the last command sent; remembering which port belongs to this Mac does not establish the current input. If the monitor or connection cannot report the active input, show it as unknown/unverified and offer an explicit recovery or destination choice rather than implying that a cached value is current. The existing **Read current input** control and pre-cycle reads are implementation starting points, not proof that this two-computer flow works. Acceptance: supported readback detects changes made by either computer or the monitor's buttons; subsequent cycling uses the actual input; failed, unsupported, or stale readback never presents the last command as verified current state; and users can recover without guessing a cycle position. Verify the complete two-computer journey with user-operated hardware checks; do not switch live inputs during the review or close this report using mocks alone.

## Implementation checkpoint — 2026-09-07

The first functional repair pass is implemented in source. The installed 1.1 app and its live helpers have not been replaced or restarted. No live privacy reset, panic, keyboard/monitor/power setting change, or publication was performed. These changes do not declare 1.2 complete.

| Priority | Finding | Source change and remaining acceptance |
|---|---|---|
| P1 | Unrelated Node/Bun programs become agent targets | Event and snapshot recognition share a conservative check of the actual supported package entry point in argv[1]. Data arguments, runtime flags/eval, similar package names, documents, and other package scripts are rejected. Synthetic event regressions cover both supported agents and runtimes. |
| P1 | Copied/running helper can be older than the app | Startup compares installed bundle identity/build. Authenticated helper status now carries the running build and protocol version; an older or unversioned helper is not reported as ready. Real upgrade/restart, signature/rollback handling, preserved grants, and clean-account acceptance remain release gates. |
| P1 | Shared monitor state becomes stale | Cycling uses fresh reliable readback or a short-lived, one-use user observation; the previous command is never a cycle position. Explicit destination buttons work without current-input knowledge. Status distinguishes monitor reports, user observations, and last requests. Known unreliable readback is rejected by both cycling and Read current input. Two-computer and monitor-button hardware acceptance remains open. |
| P1 | First authorization prompt cannot accept typing | Authorization temporarily lowers the floating Settings panel, activates the app, and suppresses delayed setup notices that could reclaim focus. Source-level focus/restoration checks pass. The reported typing failure remains open until the user operates the first prompt and cancellation/retry on both sleep paths. |
| P1/P2 | Approval text is clipped; keyboard equivalents disappear | Shared dialogs size to their contents, scroll long explanations and bodies, indicate additional text, fit full button titles, and retain native Return/Escape behavior. About exposes the build in its heading. Long-list, reachable-bottom, and retained-key tests plus light/dark renders cover the app-owned layout. Live keyboard-only/VoiceOver acceptance remains. |
| P2 | Protocol/device exploration loses monitor setup | Ordinary input selections and shortcut choices save immediately. Connection and input-list changes use a separate Save/Cancel editor, with an isolated draft and Undo after saving. Checks do not commit the proposed connection. Saved plans are retained separately per display; autosave preserves detection errors. This is storage for individual displays, not completed multi-monitor group switching. |
| P2 | Invalid Save loses the agent form | Validation and save errors retain the draft inline. Disabled shortcuts do not require modifiers. Harmless injected-save tests cover correction and retention. |
| P2 | Settings hints are dead ends; unrelated keyboard controls disappear | Monitor, scrolling, Fn, and navigation prerequisites route to their relevant page. Keyboard settings exposes built-in/external Fn and modifier controls with explicit scope. Optional navigation learning no longer hides unrelated controls. |
| P2 | Settings roots duplicate; Recheck changes keyboard settings | Reopening Settings returns to one root. Recheck reads device state without applying saved Fn/modifier choices. Physical keyboard readback/reconnect remains unverified. |
| P2 | Relearning/removing a keyboard loses useful context | A complete learned layout saves automatically. Back or closing unfinished setup keeps the old layout. Saved disconnected keyboards can be selected and forgotten individually. Tests use synthetic input and disposable storage. |
| P2 | Advanced/recovery actions have unclear context or completion | Agent additions/removals and recognition management are beside Agent Kill Switch. Advanced contains helper maintenance. Collector failure offers direct repair; successful helper protection has a result; system reset retains failed selections for retry; shortcut cleanup cannot be dismissed before its bounded wait finishes. Live privileged/destructive paths were not executed. |

The agreed main-menu refinements retain existing row styling and controls, move System metrics after common tasks/Settings, shorten repeated setup warnings, and explain the background behavior after Quit. **All-app privacy reset remains in the main menu.** No multi-monitor or lid-grace controls were added there.

Verification uses `python3 Tools/check-functional-review.py --output <scratch-directory>`, which builds a separate app with isolated configuration, preferences and helper service names, blocks AppleScript/helper installation/physical display requests, and runs 18 selected suites. A separate production-source compile covers code blocked in the test copy. The ordinary live self-test, installed app, and physical devices are not used as mutation targets.

Still open after this pass: user-operated OS/hardware acceptance above; a continuous shortcut-test flow and stop-active-controls-without-erasing-settings; the broader first-use/repair experience and accessibility acceptance; multi-monitor named destinations/groups; a fail-safe 60-second lid policy; and the distribution/upgrade/uninstall/license gates below. The current persistent lid override can survive the menu app: adding only a menu timer would not meet the required failure behavior, so the new safety policy is not advertised as implemented.

### Consistent saving and task boundaries

The user challenged the monitor-specific Apply button. The former protocol trial suspended saving for the entire monitor form, despite a label implying only the protocol would be applied; Done could then discard otherwise ordinary edits. Keeping a working setup intact justifies an editable proposal, but not a hidden change to the page’s saving rules.

- Independent ordinary controls save immediately and report their result. Back or closing leaves the page; it must not silently discard ordinary edits.
- Related setup fields that need validation together live in an explicitly separate Save/Cancel editor. Save commits the described setup; Cancel preserves the previous one. Validation/save failures retain the draft and keep the editor open.
- Read, Check, Recheck, and Detect do not commit a proposed replacement. Monitor connection checking is now separate from Use this connection, which returns a checked proposal to the setup draft. The final setup Save commits it. Editing the endpoint after a check invalidates acceptance of that check.
- Keyboard learning saves automatically when all four prompts are complete. A tested monitor result within a draft names that result, such as Use this input list, and explains when it is saved. A draft’s feedback must not replace the operating feature’s status.

Implemented for monitor settings with isolated regression coverage of immediate saving, protocol/list/connection drafts, Save, Cancel, failed Save, Undo, stale checks, and return to the ordinary page. Apply this contract in the remaining whole-app grouping/flow pass; this does not claim that every existing page already meets it.


### Reusable setup and Settings grouping — 2026-09-08

The user explicitly requested a setup process that also serves as a later completeness/recovery check. Categorized Settings alone is insufficient. Provide a permanent **Setup & status** overview, open it on first use, and keep it available after permissions are reset. Let the user leave and return without forcing every optional feature through setup. Readiness must reflect current evidence, not a record that the user once clicked a setup button.

Implemented in source:

- Settings starts with Setup & status, followed by Displays, Keyboards, Scrolling, Keep awake, Agent Kill Switch, and App settings. Login/CPU preferences are in App settings; Maintenance replaces the ambiguous Advanced label. Perch preference reset contains only Perch-owned choices. All-app privacy reset stays in the main menu.
- The overview checks background controls, input Accessibility, keyboard capability/access, monitor setup/connection, actual versus requested sleep behavior, agent shortcut/protection, and recent live collector health. Missing helpers leave dependent access unknown; optional unused features do not become failures. Display readiness does not claim a verified current input.
- Continue setup/Fix next issue leads to the first actionable repair in dependency order. Back refreshes the same overview. Reopening it from a recovery step returns to the existing overview rather than nesting another copy. Recheck performs reads; it does not request grants or apply saved device modes. Delayed startup notices no longer interrupt an already visible Settings journey.
- Keyboard settings separates ordinary Fn/modifier controls from details/troubleshooting. Navigation behavior, learning/saved layouts and app exceptions have one home. Saved layouts remain manageable without a responding input helper. Unknown/mixed checkbox values are drawn as dashes, not checkmarks. Scroll choices can be disabled after access is lost without requesting that access merely to turn them off.
- Monitor ordinary controls retain immediate saving; connection/list drafts retain explicit Save/Cancel. Navigation exceptions also makes Cancel explicit. Keep awake explains the current persistent override, distinguishes observed state from a remembered preference, and scopes its reset to sleep. It does not advertise the pending 60-second policy.
- Privacy-reset pages link back to the overview. Helper installation has a completion result and a route to check actual response/access; installation success alone is not reported as working protection.

Verification: 18 selected isolated suites, including new first-use, re-entry, missing-helper, individual permission recovery, stale status, next-step/Back, nonmutating review, category navigation and unknown-sleep-state checks. App-owned pages are rendered in light/dark appearances; the overview can show all seven checks on a sufficiently tall screen, and remains scrollable on smaller screens. A separate production-source compile covers the unmodified hardware/installation code. No installed app replacement, live privacy reset, panic, helper restart, hardware-setting change or publication was performed.

This implements the reusable setup structure and category grouping. Real first-install/permission handoffs, physical input/monitor/sleep behavior, keyboard-only/VoiceOver use, the remaining shortcut-test experience, multi-monitor groups, the fail-safe 60-second lid policy and distribution gates remain open. The earlier functional findings and user-journey reports are retained.

## Multi-monitor and lid-grace implementation checkpoint — 2026-09-08

Both requested features now have source implementations. They have not been installed, enabled on live hardware, or published. This checkpoint supersedes the earlier “pending implementation” notes; those dated checkpoints remain as review history.

- **Display groups:** explicitly include one, either, or several monitors. Give each computer a named destination with an independent port mapping per selected display. A newly discovered display is never added to a group. Duplicate models have stable identity suffixes, editable group labels, and an explicit Identify action. Disconnected members retain their labels and remain in the requested scope.
- **Current input and recovery:** Check current inputs performs fresh reads. Cycling chooses one group destination only when every member freshly reports the same saved destination. Mixed, unreadable, or unmapped inputs open the named-destination recovery path. Writes report each display as confirmed, failed, or unverified. Retry includes only incomplete members, tolerates reconnection, and rejects changed mappings. Neither atomic switching nor rollback is promised.
- **Settings contract:** ordinary group selection/activation saves immediately. The group editor commits a related set of names, members, destinations and shortcut together through Save group. Back/Close discards that draft. Saving, checking, and identifying do not switch monitor inputs. Existing menu rows remain; the existing cycle action uses an explicitly activated group.
- **Lid policy:** powered closed-lid operation has no session limit. Closing on battery or undocking while closed begins 60 seconds to open the lid. Opening cancels it. Reconnecting power cancels enforcement; five seconds of stable external power resets the next undocking interval, while brief power flapping retains the earlier deadline. The clock includes sleep and ignores wall-clock edits. Expiry releases clamshell prevention before requesting system sleep.
- **Supervision:** source adds a narrow authenticated root service and an independently observing watchdog. Menu-app leases, watchdog acknowledgment, known power/lid state, matching running-code identity, and successful control readback are needed before Ready. Late renewals do not restart stopped sessions. Restart begins disarmed and cleans up a recorded owned session. The new path does not write a permanent `pmset disablesleep=1`; a legacy override must be explicitly removed first. Quit stops supervised lid operation; ordinary input/agent/idle-awake helpers retain their existing behavior.

**P1 release gates remain.** The clamshell adapter uses Apple XNU's private `kPMSetClamshellSleepState` interface and its shared powerd reason bit. Runtime rejection/readback failure is surfaced, but simulated tests cannot establish compatibility, absence of interference, or bag-safe behavior on real Macs. Verify the first authorization prompt, complete helper installation/upgrade/cleanup, powered/battery transitions at the deadline, actual sleep/wake, supervisor/watchdog/menu failures, and supported macOS/hardware combinations before making a safety claim. Two-computer/multi-display DDC and inactive-video acceptance also remain open. Distribution, uninstall, performance and accessibility gates continue to apply.

Automated evidence: the isolated runner now has 20 suites, including multi-monitor and lid-policy/watchdog/lease/enforcement tests. It blocks physical lid/sleep calls as well as the existing physical monitor, AppleScript, helper-installation and permission mutations. App-owned group pages are rendered in light and dark appearances. Strict signature verification is checked on a complete copied helper bundle in scratch storage; installer syntax is parsed with `sh -n`, not executed. Production sources are compiled separately without launching the candidate. These are source and simulation checks, not physical acceptance.

### Local preview packaged — 2026-09-08

The user's missing UI changes were traced to the still-running **1.1 build 41**. [1.2 build 43](RELEASE-1.2.md) now packages all current functional repairs, grouped Settings and reusable setup overview, redundant-Done cleanup, display groups and supervised lid grace. All 20 isolated suites passed again; production compilation and strict signature verification passed with the same designated signing requirement as build 41. The optional `build.sh --output /path/to/Perch.app` prepares a separate candidate without overwriting the running bundle. Build 42 was reserved by an unsuccessful build with a relocated compiler cache; generated cache files were moved aside and build 43 succeeded. The existing app and helpers were not replaced or restarted. Opening the new app normally upgrades old helpers and may reapply saved input/awake choices, so packaging is distinct from a live upgrade. No special Apple feature approval is pending; physical acceptance remains our responsibility.

## Required perspectives

- Design: hierarchy, density, typography, warning prominence, light/dark and increased-contrast appearances.
- Usability and intuitiveness: discoverability, understandable labels, predictable toggles, visible current state, and first-run experience.
- Flow: one-window settings navigation, returning from child pages, permission handoffs, focus, cancellation, password prompts, and shortcut testing.
- Clarity: current state versus intended action, prerequisites versus optional features, honest status and errors, and consistent units.
- Performance: idle/menu-open/input/process-burst costs, wakeups, status publication, collector backlog, and attribution of Perch versus eslogger costs.
- Memory: bounded parsing/queues/history, resource lifetimes, long-running growth, and recovery after bursts.
- Security: least privilege, signing and helper identity, local-message authentication, file ownership, process identity checks, panic exclusions, and privacy-reset sequencing.
- Reliability and recovery: missing permissions, stale helpers, reconnect/wake, app updates, partial failures, and safe fallback behavior.
- Feature set and bloat: whether each feature serves Perch's purpose, duplicated controls, unnecessary setup, and ongoing maintenance cost.
- Accessibility and test coverage: keyboard operation, readable status beyond color alone, safe automated coverage, and explicit limits of hardware testing.

## Deliverable

Record concrete findings with severity, user impact, evidence, and recommended changes. Distinguish measured results from assumptions and hardware checks still needed. Prioritize and fix the agreed findings, then verify the resulting version. Do not label 1.2 complete solely because an automated suite passes.

## Distribution and Homebrew (requested 2026-09-06)

Investigate how other people can install and use Perch, including Homebrew publication. This is an explicit 1.2 task, not authorization to publish anything now.

- Choose an initial distribution route: a Homebrew tap with an app cask, or a source-build formula where appropriate; assess eligibility and maintenance requirements for the main Homebrew repositories later.
- Replace machine-specific identifiers, local certificate assumptions, paths and installer ownership with a distributable setup. Decide signing/notarization and release/update strategy; explain the practical limits of distributing without a paid Apple Developer identity.
- Verify installation, required permissions, collector/helper ownership, login behavior, upgrades, rollback and uninstall on a clean Mac/account. Perch must receive its own grants; never require users to weaken SIP or grant their development tools extra permissions.
- Make eslogger/macOS compatibility and any distribution/entitlement limits explicit. Confirm that the features shipped actually work on supported Macs without relying on this development machine’s grants or launch jobs.
- Audit dependencies and licenses, prepare release artifacts and checksums, document supported systems and recovery, and choose a sustainable keyboard-profile/catalog update process.
- Prepare a concrete release/cask for review before any public upload, tap publication or upstream submission.

The monitor-input hotkey was the last planned feature before this formal review, subject to monitor/cable/DDC support; see `MONITOR-INPUTS.md`. The two additional user requests recorded below are now part of the 1.2 planning scope; their implementation remains pending review.

Starting references for packaging: [Homebrew Cask Cookbook](https://docs.brew.sh/Cask-Cookbook) and [maintaining a tap](https://docs.brew.sh/How-to-Create-and-Maintain-a-Tap). A tap can carry the initial app cask while the broader signing/distribution work is completed; do not assume eligibility for the official cask repository without checking its current requirements.

## Hardware preparation research (2026-09-07)

See [hardware compatibility research](catalog/HARDWARE-COMPATIBILITY-RESEARCH.md) for the source-backed monitor/keyboard matrix, catalog limits, and prioritized implementation gates. Research is complete for this pass; it is not hardware certification or completion of the 1.2 review.


Hardware follow-up: review licensing/provenance before public packaging, collect real-device validation for catalog entries, and physically validate the new MSI USB HID / NEC LAN and serial / USB MCCS transports. Research identities are not advertised as enabled support; see catalog/vendor-protocol-research.json. Reset Settings is implemented in 1.1; review its wording and recovery flow during the broader 1.2 usability review.

## Experience standard and additional requests (2026-09-07)

The user clarified that the review must evaluate every page, dialog and transition from a first-time user's perspective, not only whether controls function. Apply the same reasoning to first run, ordinary use, adjusting a setting, fixing a problem, adding/removing devices or agents, and learning/relearning a layout. Keyboard, display/monitor, and Advanced flows need particular attention. The interface should make the next step feel natural without forcing a tutorial or requiring knowledge of Perch's internal components.

- At every step, make the affected device/feature, observed current state, effect of the next action, save status, and way back understandable. Distinguish unknown hardware state from a confirmed result; give a useful next step instead of false certainty.
- Start with working defaults and quiet detection. Ask for a permission only in context of a feature the user chooses. Basic controls must not require unrelated setup. Keep technical protocol details and maintenance behind relevant optional details.
- Use consistent, concrete action names. Checking must not secretly apply saved settings; choosing a device must not erase another device's setup; Back/Cancel must preserve the expected draft or prior working configuration. Retain edits on error and offer recovery at the point of failure.
- Review complete lifecycles, including saved but disconnected devices, removing only one custom item, relearning without losing the working layout, partial success, and returning after another computer changed shared hardware. A screenshot or passing fixture alone is not acceptance of the experience.

### Historical multi-monitor cycling scope — superseded by coordinated KVM

The September 8 KVM direction above replaces this standalone feature/acceptance
plan. Retain useful display scope, identity and partial-failure requirements in
the new design, but do not treat completing this old UI as an active todo.

Support switching one chosen external monitor, multiple chosen monitors, or a saved group such as both desk monitors. Discovering a second display must not automatically add it to a switching action. Keep each display's identity, input mappings, connection, current-input evidence and saved choices separately; selecting another display must preserve the first display's setup. Identify duplicate-model displays clearly and retain useful labels for disconnected displays.

Use a named destination and explicit scope, for example “Switch both desk displays to Mac mini,” with each monitor mapped to that computer's appropriate port. Do not independently cycle each monitor when the intended outcome is to show the same computer on both: mixed initial states can otherwise leave them on different computers. Freshly check actual input where reliable readback exists, including changes initiated by another Perch-enabled computer or the monitor buttons. Display per-monitor success, failure or unverified status. A failed/disconnected member must not silently shrink the requested scope, redirect to another monitor, or be presented as complete success. Provide an explicit retry/recovery path; do not promise atomic switching or automatic rollback without evidence.

Acceptance includes one monitor, either one of two, both together, different input codes/order per monitor, mixed initial inputs, duplicate models, unplug/reconnect, inactive video inputs, one failed/unsupported member, and an external switch from the other computer. Settings for unselected monitors remain unchanged. No live switching is authorized during the review.

### Lid-closed mode with 60-second undocking grace — source implemented; safety acceptance open

**Implementation decision — 2026-09-08:** proceed with the supervised helper implementation. No feature-specific Apple safety approval is pending. The selected private clamshell call has no Apple entitlement check in the published XNU implementation; compatibility and actual sleep/failure behavior still require our own physical testing. Administrator authorization to install the helper is separate from Apple approval. Signing/notarization concerns distribution and does not certify this feature's sleep behavior.

- [ ] **Optional research: native timed assertion shortcut.** Revisit whether Apple provides a supported, third-party-accessible assertion that prevents closed-lid sleep on battery and expires automatically. The currently identified `AppliesToLimitedPower` and `AppliesOnLidClose` options require Apple-internal entitlements, so do not assume Perch can obtain or use them. We may not need this shortcut if the supervised implementation proves reliable; it is not a prerequisite for the current implementation or local build. A supported replacement could reduce custom cleanup/watchdog work. References: [Apple assertion definitions](https://github.com/apple-oss-distributions/IOKitUser/blob/main/pwr_mgt.subproj/IOPMLibPrivate.h), [XNU clamshell dispatch](https://github.com/apple-oss-distributions/xnu/blob/main/iokit/Kernel/RootDomainUserClient.cpp), and [notarization scope](https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution).

Purpose: keep the Mac working with its lid closed at a powered desk, then give the user 60 seconds to grab it from the dock and open the lid. If it remains closed on battery when that time expires, it must sleep. The undocking grace period is primarily time to open the Mac; reconnecting power can also cancel the deadline. Powered lid-closed operation has no 60-second session limit.

- When this mode is enabled and the lid closes while external power is present, stay awake while that condition holds.
- Entering the lid-closed, on-battery state starts a 60-second grace period, whether the user closes the lid on battery or unplugs/undocks during powered lid-closed operation. Undocking must allow time to carry and open the Mac. If it remains closed on battery at expiry, actually put the Mac to sleep; merely clearing a UI toggle or an idle-sleep assertion does not satisfy the requirement.
- Opening the lid cancels the pending deadline and restores ordinary lid-open behavior. Reconnecting external power before expiry also cancels the deadline and permits powered lid-closed operation. Closing the lid again starts a new close event. Connecting power after the Mac has already slept must not be treated as an instruction to wake it.
- Do not let repeated status updates or power flapping extend an unpowered deadline indefinitely.
- Define external power from the actual power-source state, not battery percentage or a “charging” label; a full battery may still be externally powered. Unknown power/lid state must not authorize indefinite lid-closed wakefulness.
- Show the policy clearly when enabling it: “Stay awake with the lid closed while connected to power. After undocking, you have 60 seconds to open the lid before the Mac sleeps.” Report states such as “Ready · lid-closed operation requires power,” “On battery · sleeping in 43 seconds unless you open the lid or connect power,” and “Lid closed · running on external power.” Authorization is required to install the helper. A new session can start with the lid open or closed on confirmed external power. Failure to arm or verify enforcement must not appear as Ready.
- Investigate a fail-safe implementation for sleep enforcement across menu-app exit, helper failure/restart, sleep/wake, stale events, and loss of authorization. The current persistent lid override plus ordinary keep-awake assertion must not be assumed sufficient. Resolve how actual sleep is requested and verified without terminating unrelated processes or silently leaving a no-sleep override behind. Do not advertise this as a safety guarantee until the failure cases are demonstrated.

Acceptance requires deterministic state/deadline tests plus user-operated hardware checks for closing plugged in, closing on battery, undocking while closed then opening within 60 seconds, remaining closed on battery after undocking, opening just before/after expiry, reconnecting power just before/after expiry, repeated lid/power changes, power-state uncertainty, and helper/app failure. Do not change live sleep, power, lid or hardware settings during the review.


### Leaving Settings pages — 2026-09-08

User correction: ordinary pages already have the shared Back/Close control and window close button, so an additional Done button is redundant and suggests a saving step that does not exist. Removed Done/Done for now from the overview, ordinary monitor page, input-access page and completed event-collection setup. The earlier navigation-layout change still retained Use this layout and Cancel setup; that did not satisfy the user's intent. The follow-up correction removes both: completing all four key prompts saves automatically and shows only Back as the page action. Back or closing unfinished learning preserves the previous layout. Failed saves retain the old layout and show Retry saving. Navigation app-exception choices also save immediately with Back. Explicit draft editors and destructive confirmations retain their separate action semantics.
